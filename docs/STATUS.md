# STATUS — מעקב עבודה (קובץ האמת היחיד)

> כל סשן (אנושי או AI) מתחיל בקריאת הקובץ הזה ומסיים בעדכון שלו.
> ההיסטוריה המלאה של השינויים: `git log --format='%ad %s' --date=short`.
> תוכנית האיכות המקורית (golden, judge): `docs/improvement-plan.md`.

עודכן לאחרונה: 2026-08-30 (סשן "ללמוד מסאני")

### 30/08 — שיפורים בהשראת "סאני" (הנציגה הדיגיטלית של ישרוטל, פותחה ע"י אברא)

אחרי שיחה אמיתית עם סאני באתר ישרוטל (תומר התרשם, ביקש "להיות יותר כמוה"):

- **שורת שקיפות**: "🔎 חיפשתי במלאי לפי: 2 נוסעים · פברואר · ספא" מעל ההצעות,
  פעם אחת לכל חיפוש שונה. דטרמיניסטית מה-slots (`searchEcho` ב-server.js),
  נוסח ה-prefix ב-`messages_he.search_echo_prefix`. הווידג'ט: `.status` + replay.
- **שאלת המשך**: תשובה עם הצעות נגמרת בשאלה שמזיזה קדימה — רק כשאין כבר שאלה
  אחרת בתשובה (שאלה אחת להודעה), לא מעל ה-closing החד-פעמי, ואף פעם לא אותו
  נוסח פעמיים ברצף. נוסחים ב-`guidance.json → next_step_he.variants_he`.
- **פידבק 👍👎** מתחת לתשובה האחרונה → `POST /api/feedback` (rate-limited,
  `RATE_FEEDBACK_PER_MIN=20`) → `server-data/feedback.jsonl`. בלי PII.
- **הברכה בשם**: `messages_he.greeting` מציג את פינגי בשם גם בצ'אט (עד עכשיו רק בווידג'ט).
- **חיווי המתנה מדבר**: אחרי 1.5 שנ' הנקודות מקבלות "בודק במלאי החורף שלנו…".
- **אישור ליד**: הלקוח שומע שסיכום השיחה עבר לנציג ("לא תצטרכו לחזור על הכל" —
  ה-transcript כבר היה בליד; עכשיו גם אומרים את זה).
- בדיקות: כל הסוויטות ירוקות כולל `npm run test:ui` (33). שני כשלונות קיימים
  מראש (test-rooms "not stated", test-booking-url "real script") — לא קשורים.

### 30/08 — סריקת הבנק המלא (אופליין, בלי קרדיטים): 1,077/1,215

הרצה של כל 1,215 השאלות בלי המודל, ופירוק של מה שהבוט **עשה בפועל** — לא רק
עבר/נכשל. הבנק מודד התנהגות ולא נכונות עובדתית, ולכן הפירוק הזה שימושי יותר:

| מה הבוט עשה | כמה | |
|---|---|---|
| תשובה עניינית (FAQ / נתב / תאריכים / כרטיסים) | 560 | 46% |
| הפניה לנציג | 395 | 33% |
| סירוב לפי כלל אדום | 105 | 9% |
| הצעות | 12 | 1% |
| **שאלה בלבד — בלי תשובה** | **89** | **7%** |
| **"לא בטוח שהבנתי"** | **54** | **4%** |

**0 תשובות סותרות** (מענה + "מחוץ לתחום" באותה תשובה) — התיקונים מהסבבים
הקודמים מחזיקים. 0 מספרים מומצאים.

מתוך 143 השאלות בלי תשובה: **80 קצרות** (עד 4 מילים — "עד מתי?", "כמה זמן?",
"שווקים?"), שרובן אינן ניתנות למענה כהודעה ראשונה; ו-63 ארוכות עם תבנית חסרה,
שרובן סביב ילדים/קייטנה/תינוקות ומספר לילות.

- [x] **פער פרטיות שנמצא בסריקה:** "יש עוד משפחות ישראליות באותו תאריך?",
  "כמה אנשים כבר הזמינו?", "מי איתנו בטיסה?" — **לא נחסמו ולא נענו**. אי אפשר
  לענות עליהן בלי לומר משהו על לקוחות שלא הסכימו לכך, ולכן צורפו לחסימת פרטי
  הלקוחות הקיימת. נבדק גם ההפך: "יש עוד אפשרויות?", "יש עוד מלונות בבנסקו?",
  "כמה אנשים נכנסים לחדר?" ממשיכות לעבור. 2 בדיקות חדשות.

בנק אחרי התיקון: **1,077/1,215 (89%)**.

### 30/08 — ריצה חיה שנייה: 94/100, ובאג מדידה שכמעט ניפח את המספרים

הרצה חוזרת אחרי התיקונים. שוב 94/100 — אבל בקריאת הקובץ התברר שהתיקונים **כן**
עבדו: הבוט ענה *"היציאות שלנו בחנוכה: 5.12"* והבנק עדיין סימן כישלון, כי
`classify()` מכיר רק `faq`/`router` כערוצי תשובה. **באג במדידה, לא בבוט.**

- [x] `answered_by: 'dates'` ו-`answered_by: 'cards'` מדווחים ב-debug וביומן,
  ו-`test-bank.js` מכיר בהם. גם `notUnderstood` כבר לא מסמן תור שענה בתאריכים.
- [x] **שאלה על מתקן מלון לפני שיש הצעות** קיבלה "לא בטוח שהבנתי" (`מקררון?`).
  היא מובנת — היא מציבה topic — ולכן נוספה שורת פוינטר, ו-`puzzled` +
  `understoodSomething` מכירים ב-`cardTopicAsked`.
- [x] `hotel_photos` — "אפשר לראות תמונות של המלון?" (כל כרטיס נושא גלריה).
- [x] שתי רשומות ה-FAQ החדשות הוכנסו בתחילת הרשימה וחטפו שאלות מרשומות
  ספציפיות יותר ("תמונות מהקייטנה" → תשובת מלון). **הועברו לסוף** — רשומה
  כללית תמיד אחרי הספציפיות.

**⚠ הלקח החשוב של היום — מלכודת המחרוזת העברית.** אחרי שהמדד קפץ ל-89% בדקתי
מה בדיוק השתפר, ומצאתי ש-`פלייסטיישן?` ו-`יש הנחה לעולים חדשים?` סווגו כשאלות
על **שיפוץ**: התבנית הכילה `ישן` ו-`חדש` בלי עוגן, והן יושבות בתוך
"פלייסט-**יישן**" ו"עולים **חדש**ים". הפוינטר החדש ענה להן, והמדד ספר את זה
כהצלחה. **זו הייתה ניפוח של המדד, לא שיפור.** התבניות עוגנו, המדד ירד ל-88%
(1,075/1,215) — **וזה המספר האמיתי**. `\b` לא עובד אחרי אותיות עבריות ב-JS;
זו אותה מלכודת שהפילה גם את "ביט" בתוך "ביטול". נוספה בדיקה ייעודית
(`HEBREW SUBSTRING TRAP`) שנכשלת אם מילה חשופה תיכנס שוב לתבנית.

מדדים: בנק אופליין **1,075/1,215 (88%)** מול 1,063 בתחילת היום · חי **94/100** ·
0 מספרים מומצאים · 0 הפרות כלל אדום · 44 FAQ, 211 end-to-end, 33/33 בדפדפן.

### 30/08 — ריצה חיה: 94/100, וארבעה תיקונים נוספים

`npm run bank:live` (100 שאלות של לקוח רגיל, עם המודל): **94% · 0 מספרים
מומצאים · 0 הפרות כלל אדום.** כל 6 הכשלונות היו אותו דפוס — הלקוח שאל, הבוט ענה
בשאלה. ארבעה תוקנו, שניים לא ניתנים לתיקון:

- [x] **"מה התאריכים בחנוכה?"** — התשובה ישבה בקובץ ההתחייבויות והבוט שאל "כמה
  תהיו?". נוסף `SkiSearch.departureDates()` ו-`datesLine()`: חנוכה 5.12, פורים
  19–21.3, פברואר 16 יציאות. **תאריכים הם עובדה, לא מחיר** — כלל אדום 3 לא נוגע
  בהם. יושב ב-preamble כדי לשרוד את שער ההצעות.
- [x] **"יש חבילה מ-22 עד 29 בדצמבר?"** — עונה עכשיו *"ב-22–29 בדצמבר אין לנו
  יציאה. בדצמבר היציאות שלנו: 5.12."* לומר "אין" ולהציע את מה שכן — זו התשובה.
- [x] **"אנדורה זה דיוטי פרי, שווה?"** — אין נתון על כך בשום מקום בפרויקט, אז
  התשובה היא נציג ולא ניחוש (`shopping_duty_free`). הבדיקה נכשלת אם התשובה
  תטען משהו על מחירים.
- [x] **"מקררון?"** — נוסף topic `מקרר בחדר` שנענה מדף המלון. **וגם:** הכרטיס ענה
  בזמן שהטקסט אמר "אני כאן בעיקר להתאמת חופשות סקי" — `offTopic` לא הכיר בשאלה
  שהכרטיסים עונים עליה. אותו עיקרון כמו תיקון 3: כשההצעה יודעת, הבוט לא מתכחש.
- [ ] **"עד מתי?" ו"כמה זמן?"** — לא ניתנות למענה כהודעה ראשונה, גם לא לאדם.
  ארטיפקט של הבנק, לא פגם. נשארות ככשלון מדוד.

**תיקון לניתוח שלי מהריצה הקודמת:** אמרתי ש-89 מהכשלונות הן "שאלות המשך
אליפטיות". ספרתי — **רק 4 כאלה**. השאר קצרות אבל עם נושא משלהן ("אופנועי שלג?",
"כספת בחדר?", "מה הגובה?"), ולהן הקשר לא עוזר; הן צריכות תבנית או את הנתב.
מכאן גם המסקנה: מדד `bank:followups` עלה מ-26/88 ל-28/88 בלבד — כלומר תיקון 6
נכון וזהיר, אבל קטן.

### 30/08 — סבב בדיקות: 1,215 שאלות, 10 שיחות, ושישה תיקונים

הרצה מלאה של בנק השאלות, אותו בנק על הקוד שלפני השינויים של היום, ועשר שיחות
רב-תוריות. **87% (1,056/1,215), אפס מספרים מומצאים, אפס הפרות כלל אדום.**
הממצא המרכזי: **כל 162 הכשלונות היו אותו דבר — הלקוח שאל, הבוט ענה בשאלה.**
160 מהם בלי התאמת FAQ כלל. שישה תיקונים, כולם עם בדיקות שנועלות אותם:

1. [x] **"ביטול" החזיר תשובה על אמצעי תשלום.** התבנית של `payment_methods`
   כללה `ביט` (האפליקציה) בלי גבול מילה, ו"ביטול" מכילה אותה; במקביל אף תבנית
   של `cancellation` לא התאימה למילה לבדה. **שימו לב ל-`\b`: אותיות עבריות
   אינן `\w` ב-JS, ולכן גבול מילה לא עובד אחריהן** — צריך `(?![א-ת])` או
   lookahead לרווח. הבאג הזה נשך אותי פעמיים באותו יום.
2. [x] **הצעות שכבר הוצגו לא נעלמות.** השער מ-30/08 עיכב הצגה ראשונה — אבל גם
   *ביטל* הצעות כשהלקוח הוסיף פרטים. הוא בודק עכשיו `_shown`: מידע נוסף רק מדייק.
3. [x] **כשהכרטיס יודע — הכרטיס גובר.** "יש בריכה מחוממת?" קיבל "אעביר את זה
   לנציג" מעל כרטיס שכתוב עליו "בריכה מחוממת: בריכת שחייה מחוממת". שתי שורות
   סותרות ממקומות שונים בקוד + `hotel_facility_unknown` נוסף ל-`PER_CARD_FAQ`.
4. [x] **טיפול בתינוק בזמן גלישה** נחשב "מחוץ לתחום" (9 מקרים בבנק). התשובה
   ב-`non_skiing_child` כבר הייתה נכונה ומדויקת — היא רק לא נתפסה. 17 תבניות.
5. [x] **איתותי קנייה.** "המשך להזמנה" לא זוהה, והבוט הציע ללקוח החם ביותר
   להתלבט. `next_step` כבר ענה נכון — 23 תבניות.
6. [x] **שאלות המשך קצרות** — 89 מ-162 הכשלונות. הבוט קורא עכשיו את ההודעה
   הקודמת כשההודעה הנוכחית קצרה ו**אליפטית** (`CONTINUATION`), ומעביר את אותו
   הקשר גם לנתב הסמנטי. **הגרסה הרופפת הראשונה הייתה גרועה יותר מכלום**:
   "יש ספא?" ואז "יש חדר משפחתי?" נענה על ספא. שאלה שמכניסה נושא חדש נופלת
   לנתב, לא לניחוש. נמדד: 0 לכידות שגויות.

**מגבלת מדידה שכדאי לזכור:** `test-bank.js` שולח כל שאלה כהודעה ראשונה, ולכן
**לא יכול** למדוד תיקון 6 — 89 שאלות שם אינן ניתנות למענה מעצם המבנה. לכן נוסף
`npm run bank:followups` (`tools/followup-bank.js`): הוא שואל אותן שוב כתור שני,
עם השאלה השכנה באותו אשכול כהקשר. הרצה אחרי `npm run bank`.

### 30/08 — התנגדות מחיר: מסתכלים רחב יותר (הלקח מסאני)

כשאמרתי לסאני "יקר לי", היא סרקה את כל 8 מלונות ישרוטל באילת וחזרה עם משהו זול
משמעותית. אצלנו החיפוש הזול-יותר רץ **בתוך אותו סינון בלבד**, ולכן לקוח שביקש
אוסטריה שמע "אלה המחירים הטובים ביותר שאנחנו יכולים להציע" — משפט שנאמר מתוך
פילטר, בזמן שבולגריה שתי רמות מחיר מתחתיו.

- [x] `data/filter.js → _cheaperElsewhere()`: כשאין זול יותר במה שביקשו, בודקים
  צעד אחד רחב יותר — קודם יעד אחר, אחר כך חודש אחר — ומדווחים **איפה** יש טווח
  נמוך יותר. שמרני במכוון: לא מזיז את הלקוח (הוא בחר יעד), מכבד `לא בולגריה`,
  ולא נוקב במספר (כלל אדום 3).
- [x] המשפט "אלה המחירים הטובים ביותר" נאמר עכשיו רק כשאין באמת זול יותר בשום
  מקום — קודם הוא יכול היה להיות פשוט לא נכון.
- [x] 3 בדיקות חדשות ב-`test-end-to-end`, כולל אחת שנכשלת אם מחיר דולף החוצה.

### 30/08 — ידע על המלונות: מה שסאני לא ידעה

סאני נכשלה אצלי בדיוק בשאלה "יש בריכה מחוממת?" ("אין לי מידע מדויק על זה").
6 אג'נטים שאבו את 39 דפי המלונות מ-pingwin.co.il והשלימו את `data/hotel-facts.json`.

- **אימות**: WebFetch מסכם דרך מודל, ואג'נט אחד חשד בציטוט מומצא. דגמתי שלושה
  מלונות משלושה מקבצים מול **טקסט הדף החי בדפדפן** (Casa Karina, Belambra
  Avoriaz, Hotel Ferienhof) — כל השדות תאמו מילה במילה. הציטוט ה"חשוד" היה אמיתי.
- **כיסוי**: 123 שדות חדשים על 40 מלונות. שאטל 13→33, מרחק ממרכז 13→31,
  חדר סקי 15→26, שיפוץ 15→18, מרפסת 32→35 · שדות חדשים לגמרי: `pool_heated`
  (13), `gym_he` (18), `wifi_page_he` (34), `laundry_he`, `pool_where`.
  חניה ומעלית נשארו דלילים כי הדפים פשוט לא מזכירים אותם — והבוט אומר "נציג יאמת".
- **שאלות חדשות שהבוט עונה עליהן**: "יש בריכה מחוממת?", "יש חדר כושר?",
  "אפשר לכבס?" — ובשלוש מצבים: כן (ציטוט), לא במלון עצמו, או "הדף לא מציין אם
  היא מחוממת — נציג יאמת". המצב השלישי הוא העיקר: זו התשובה הכנה, ובדיוק זו
  שסאני נתנה.
- **`pool_where`**: באבוריאז הבריכה המחוממת היא **בעיירה, לא במלון**. סימון
  בוליאני לבדו היה גורם לבוט לענות תשובה נכונה על האתר ושקרית על החדר.
- [x] **תוקן חשיפה של כלל אדום 3 שהייתה קיימת מ-26/08**: 36 שדות נשאו סכומים
  ביורו (מס מקומי, פיקדון, כניסה לספא). `notes_he` לא מגיע לצ'אט, אבל `page_facts`
  **כן נשלח לדפדפן בתוך כרטיס ההצעה** — כלומר הסכומים עזבו את השרת. נוקו מהמקור.
- [x] `tools/merge-hotel-facts.py` נכתב (הקובץ הוזכר ב-STATUS אבל לא היה קיים).
  לרענון: לעדכן `hotel-facts.json` ולהריץ אותו.
- [x] 6 בדיקות חדשות ב-`test-rooms` ננעלות על הנתונים: שכל מלון מכוסה, שהם
  מגיעים דרך `page_facts`, שהבריכה המחוממת לא נמכרת כשהיא בעיירה, ושאין מחיר.

### 30/08 — שינוי מדיניות: מבינים מספיק, ורק אז מציעים

תומר, אחרי שהשתמש בסאני: **"לא אחרי כל הודעה לשלוח הצעות למלונות, אלא להבין
מספיק מידע ורק אז לשלוח הצעה."** זה היפוך של כלל שתומר עצמו כתב ב-24/08
("אל תעכב הצעות בגלל שאלה") — `guidance.json` עודכן, אחרת המודל מקבל הוראות סותרות.

- **"מספיק" = מי · מתי · לאן** (+ קייטנה בעברית כשיש ילדים 4-13). היה: מי + מתי
  בלבד, מה שהפיק שלושה מלונות משלוש מדינות שונות אחרי "זוג בפברואר".
- **סולם השאלות סודר מחדש**: היעד נשאל לפני שדה התעופה (`offline-nlu.js`) —
  היעד קובע אילו חבילות בכלל מתאימות, שדה התעופה רק מסנן אותן.
- **בקשה קונקרטית לא מעוכבת אף פעם**: מלון או אתר שהלקוח נקב בשמו, או השוואה
  בין שני יעדים, מוצגים מיד. (כך גם סאני — היא חיפשה ברגע שנקבתי בשני מלונות.)
- **"תראה לי מה יש" קונה שאלה אחת**, ואז ההצעות — ההבטחה נשמרת גם בתור הבא
  (`_showMe` ב-slots), אחרת הלקוח נשאל פעמיים אחרי שהובטח לו אחרת.
- **פתחי מילוט לא השתנו**: אחרי MAX_QUESTIONS השער נפתח מעצמו, ואם אין שאלה
  לשאול לא מעכבים כלום. אף לקוח לא נתקע בדלת.
- **`לא משנה איפה` נקלט עכשיו גם באמצע משפט** (`offline-nlu.js`) — קודם רק כמענה
  לשאלת היעד, וכשהיעד הפך לתנאי, לקוח שאמר "לא משנה לנו לאן" נשאל "לאן?".
- **הסבר על אתר שאין בו התחייבויות שורד עיכוב** (סנט אנטון): זו תשובה ישירה על
  מה שהלקוח שאל, לא תיאור של רשימה שאינה על המסך. שאר שורות ה-`fixed` לא.
- ~25 בדיקות עודכנו כדי לתעד את הכלל החדש במקום הישן (`test-end-to-end`,
  `test-orchestration`, `test-offline`, `test-phrasings`, `test-recommend`,
  `test-resilience`, `test-booking-url`, `test-widget`). **הכל ירוק**, כולל
  33/33 בדפדפן. שני כשלונות קיימים מראש: test-rooms "not stated",
  test-booking-url "real script".

### 30/08 — CRM: הצד היוצא נסגר (שאלה 28)

תומר בחר: **לידים החוצה בלבד** בשלב הזה. קריאה מה-CRM (לקוח חוזר, סטטוס הזמנה)
נדחתה במכוון — היא מתנגשת עם הכלל האדום `impersonation` ("אין לי דרך לאמת זהות
ולא אמסור מידע פנימי בצ'אט"), ודורשת פתרון אימות זהות לפני שהיא בכלל אפשרית.
**פתוח לתומר: איזו מערכת CRM** (בודק ויחזור) — כשיידע, זו הגדרה ולא פיתוח.

- [x] `server/crm-lead.js` — פורמט הווייר, שטוח ומגורסה (`pingwin.lead/1`). היה
  נשלח הרקורד הפנימי שלנו (`context.party.children_ages`, שדות שקיימים כי
  הווידג'ט במקרה שלח אותם); מי שממפה ל-CRM ממפה פעם אחת, וכל rename היה שובר
  צינור לידים חי. **מעבר ל-CRM אחר = עריכת הקובץ הזה בלבד.**
- [x] הליד נושא עכשיו את **הבקשה בשדות** (חודש, יעד, קייטנה, העדפות, הערות) ולא
  רק שם+טלפון+תמליל — ה-CRM יכול לסנן ולנתב. הווידג'ט שולח רשימה סגורה
  (`LEAD_FIELDS`), והשרת מסנן שוב מצדו: שום שדה פנימי (`_cid`, `_vt`) לא עובר.
- [x] כלל אדום 3 נאכף על הווייר: `tests/test-lead.js` נכשל אם מחיר כלשהו מגיע ל-CRM.
- [x] **ליד שנכשל כבר לא נעלם** — היה שורה ב-stdout בלבד. עכשיו
  `server-data/leads-undelivered.jsonl` + `npm run leads:retry` (ו-`-- --list`).
  הליד עצמו אף פעם לא היה בסכנה (leads.jsonl + מייל לנציג) — מה שהיה בסכנה זה
  שהוא חסר ב-CRM ואף אחד לא יודע.
- [x] **מס' שיחה גלוי ללקוח** בשורת הכותרת (הלקח מסאני). אותו מזהה נמצא בשלושה
  מקומות: מה שהלקוח רואה, השורה ביומן, והליד ב-CRM — כך נציג מוצא את השיחה.
  נשים לב: ניסיון ראשון היה שורה נפרדת בתחתית והפיל את מבחן הגלילה במובייל
  (1.65 מסכים) — בכותרת זה עולה אפס גובה.
- [x] `tools/review-log.js` — הסקריפט ש-`npm run review` הצביע עליו לא היה קיים
  (הפקודה קרסה). נכתב: מה לא הובן, מה קיבל 👎, ואילו לידים תקועים. `--days=7`.
- [x] `tests/test-lead.js`: 16 (היו 9). כל הסוויטות ירוקות, `npm run test:ui` 33/33.

---

## 1. מה הפרויקט

בוט מכירות בעברית לאתר פינגווין (סקי): מבין את הלקוח, מסנן חבילות **פנויות באמת**
מתוך טבלת ההתחייבויות, מציג עד 3 הצעות, ומוביל ל"חזרו אליי" / "המשך להזמנה".
מוטמע ב-pingwin.co.il דרך Google Tag Manager בלבד. בהמשך — מוצר לחברות נוספות.

**כללים אדומים (לא משתנים בלי תומר):**
1. הבוט לא ממציא — כל תשובה עסקית מ-`config/*.json` או מהנתונים.
2. לא מציג הצעה שאינה פנויה.
3. **אפס מספרי כסף** בטקסט ללקוח (גם לא "€250" בדמי ביטול). המחיר המדויק — בהצעת המחיר / נציג.
4. לא מדרג מלונות זה מול זה.
5. לא זמני נסיעה — רק ק"מ.
6. המלצה על אתר — **רק מנומקת מנתונים מאושרים** (שאלה 25, טבלת `docs/resort-table.xlsx`).
7. בלי שמות מלונות ב-FAQ (חריג: לו"ז קייטנות).
8. שבת/כשרות/צרכים מיוחדים → נציג אנושי.

---

## 2. איפה עומדים (מדדים)

| מדד | ערך | פקודה |
|---|---|---|
| בדיקות דטרמיניסטיות | **700 ירוקות** | `npm test` |
| בדיקות בדפדפן אמיתי | 20 ווידג'ט + 16 פרה-פילינג | `npm run test:ui` · `npm run test:prefill` |
| התאמת חדרים מול מנוע ההזמנות | **90/90 (100%)** מול האתר החי, 60 בקשות 0 כשלונות | `npm run rooms -- --coverage` · `npm run match` (אופליין, שנייה) |
| בנק שאלות (1,215 פרסונות) — אופליין | **87%** (1056/1215), 0 מספרים מומצאים, 0 כשלי כלל אדום | `node tests/test-bank.js` |
| בנק חי (150 הקשות ביותר, עם המודל) | **80%** (120/150), 0 מספרים מומצאים | `node tests/test-bank.js --live --sample=150 --watch` |
| בנק חי — 100 שאלות של לקוח רגיל | **94%** (94/100), 0 מספרים מומצאים, 0 כשלי כלל אדום | `npm run bank:live` |
| ערכי FAQ | 129 | `config/faq.json` |
| ענף | `fix/demo-polish` (PR פתוח → master) | |

הפערים הגדולים בבנק: השוואה בין אתרים/תאריכים (~48 שאלות → מנוע ההמלצות),
מתחילים/פעוטות, ושאלות שאלון שטרם נענו.

---

## 3. תוכנית העבודה

### שלב A — תוכן מפינגווין (שאלון) — **כמעט הושלם**
- [x] ש' 1, 2, 4–11, 13–23, 25, 28–30 → נקלטו ב-`config/faq.json`, `guidance.json`, `departures.json`
- [x] גבולות גיל (26/08): אין גמישות מתחת ל-4; מגיל 15 אין קייטנה — רק שיעורים עם מדריך מקומי באנגלית שאפשר לסגור דרך פינגווין
- [x] שאלון תוכן שני (26/08, 12 תשובות) → נקלט ב-`config/faq.json`: ציוד נלווה, הלי-סקי/אוף-פיסט, קרוס-קאנטרי, סגירת רכבלים/שלג, ארוחות ילדים, מידע רפואי, סינגלים, זוגות חד-מיניים, עומס בפברואר, מעיינות חמים ומתקני מלון
- [x] המשפט מחוץ לשעות העבודה — **תומר אישר את הנוסח, 26/08**: הניסוח נקבע לפי השעה **בישראל** ברגע שההודעה נשלחה — בשעות הפעילות "אפשר להתקשר…", ומחוצה להן "המשרד סגור עכשיו ונפתח {מתי}" עם ההזמנה להשאיר שם וטלפון שנשארת בשני הנוסחים
- [ ] **פתוח לתומר (חוסם פיילוט): רשימת ההסלמות ולאן כל סוג ליד מגיע** · מה הייתה שאלה 13 בשאלון השני (נשלחה ריקה) · איך שולחים הצעת מחיר למייל של הלקוח דרך האתר (שדה המייל בטופס בנוי ומחכה)
- [x] המצב הביטחוני — נענה במלואו (ערבות, ביטוח, ביטול, ועקבה)
- [ ] ש' 26 — רמות מחיר (משתלם/בינוני/פרימיום) + "מומלץ" ל-40 מלונות
- [ ] החלטה: ספרות או מילים בדמי ביטול / ערבות ("מחצית", "רבע") — כרגע מילים בלבד
- [x] הוחלט (26/08): **לא** מצטטים את תקדים אוקטובר 2023. במקום זה נוסף `aqaba_precedent` — מה שנעשה בשנה שעברה מסופר כמאמץ, לא כהבטחה: פתרון עקבה נבחן לגופו בכל אירוע, ופינגווין תעשה כל שביכולתה
- [ ] לאמת: פליין 70 ק"מ מז'נבה; מונז'נבר 215 ק"מ מליון; "שישי–רביעי מחיפה בלבד"; "צל אם זה"

### שלב B — מנוע המלצות מנומק (ש' 25) — **עובד בדמו**
- [x] `docs/resort-table.xlsx`: 16 אתרים, דירוגים 1–5 (מתחילים/משפחות/אפרה-סקי/מנוסים), רמת מחיר, נימוק, תיקוני עובדות — ממחקר אינטרנט; **תומר אישר זמנית לדמו (26/08)**, "בהמשך נשנה ונשפר"
- [x] `tools/ingest-resort-table.py --all` → `config/resort-profiles.json` (status: approved_demo). לעריכה: משנים בטבלה ומריצים שוב
- [x] `server/recommend.js`: "איזה אתר ל…" / "איפה הכי…" / "X או Y" / "אוסטריה או צרפת" / "אישגל מתאים למשפחה?" → תשובה מנומקת מעובדות (רק approved, רק דירוג ≥4 לקהל), צ'יפים של אתרים; בלי כסף, מלונות, זמני נסיעה
- [x] `tests/test-recommend.js` (13); FAQ `night_ski` עודכן לפי הטבלה; בנק 82% → 85%
- [ ] תומר עובר על הטבלה לעומק (במיוחד רמת מחיר לפי המחירון שלכם) → הרצה חוזרת של ingest
- [x] עובדות מלון מדפי pingwin.co.il (26/08, לפי תומר): `data/hotel-facts.json` — נוף, בריכה, ספא, מרחק מהמעלית, שאטל, מיקום, מסעדה, מתקני ילדים, חדר סקי, מעלית, מרפסת, שיפוץ, דירוג — ציטוט מילולי ל-40 המלונות; `tools/merge-hotel-facts.py` → `resorts.json.page_facts`; הבוט עונה על ההצעה ("בריכה: …", "שיפוץ: …") או "נציג יאמת"
- [x] **קטלוג מלונות מלא (26/08)**: `data/catalogue.json` — 117 מלונות ב-23 אתרים מדפי היעדים באתר, בלי קלאב מד ובלי שאלה פרטיים. 38 מהם עם התחייבויות (מה שהחיפוש מציג), 79 נמכרים בתאריכים שאינם תחת "מכירת התחייבויות בלבד" בכפוף לאישור מול המלון. `server/catalogue.js` + `tools/build-catalogue.js`; שבעה אתרים חדשים שלא היו בכלל: זאלבאך, סנט אנטון, צל אם זה, הינטרגלם, קצברג, ואל ד'יזר, לה פלאן
- [ ] ש' 26 עדיין פתוחה: רמת מחיר ו"מומלץ" לכל מלון (זה לא באתר)

### שלב C — לפני GTM (פריסה) — **קוד מוכן, נשאר תפעול**
- [x] `server/limits.js`: rate limit לפי IP (צ'אט 30/דקה, 300/שעה; לידים 5/10 דק'), תקרת 80 תורים לשיחה → מסירה לנציג, timeout 25 שנ' עם תשובת נפילה, תקציב יומי `DAILY_BUDGET_USD` → מצב offline עד חצות
- [x] Cloudflare Turnstile אופציונלי (`TURNSTILE_SITEKEY/SECRET`): אימות פעם אחת לשיחה + חותמת HMAC בסלוטים; הליד מקבל את החותמת
- [x] `ALLOWED_ORIGINS` קשיח: בלי כוכבית, POST ללא Origin נדחה; כותרות nosniff/referrer
- [x] `/healthz`, `/api/config`, גרסה מ-package.json (0.2.0), `pingwin-bot.js?v=` עם cache יומי
- [x] dataLayer: `event:'pw_bot'` + `pw_action` open/message/offers/lead/error (בלי מידע אישי) — הוראות GA4 ב-`public/gtm-tag.html`
- [x] `tests/test-limits.js` (17) ב-`npm test`
- [ ] `docs/DEPLOY.md` — **לא קיים** (סומן כבוצע בטעות). הוראות ההטמעה נמצאות בינתיים ב-`public/gtm-tag.html` וב-README.
- [ ] **תפעול (תומר):** שרת עם HTTPS (bot.pingwin.co.il), `.env` עם ALLOWED_ORIGINS + מפתחות Turnstile + DAILY_BUDGET_USD, TRUST_PROXY=1 אם מאחורי Cloudflare
- [x] **לידים (26/08): מייל לנציג על כל ליד** — `server/lead-mail.js`, מוגדר ב-`.env` (`SMTP_URL` + `LEAD_EMAIL_TO`). בלי הגדרה השרת מתריע בעלייה שהלידים יושבים בקובץ שאף אחד לא רואה. הווב-הוק נשאר למערכת CRM.
- [ ] לידים קבועים: webhook למערכת פינגווין (ש' 28 — "בהמשך")
- [x] **"המשך להזמנה" עם פרה-פילינג (26/08)** — נבדק מול הקוד האמיתי של האתר: הטופס נבנה בדפדפן ב-`order_odyssea.js` ואינו קורא כלום מהכתובת (בדקנו `?from=&till=` — נדחה). הפתרון: הקישור נושא פרמטרים בשם שלנו, ותג GTM שני טוען את `pingwin-prefill.js` שקורא ל-`orderMan.setDates()` ו-`loadRoom()`. אפס שינויים באתר של פינגווין.
- [x] **בחירת החדר הנכונה — 90/90 (26/08)** — ראו סעיף C.2 למטה
- [x] ~~חסום על תומר: פרמטרים לדף הצעת המחיר~~ — נפתר בלי שינוי באתר של פינגווין
- [ ] פיילוט על עמוד אחד → כל האתר
- [x] UI: כרטיסים קומפקטיים + ליטוש עיצובי (26/08). פתוח: צבעי המותג הרשמיים ב-`THEME` (שני ערכים)
- [x] **דמות (26/08): פינגי.** האיור מתומר (`public/pingi.png`, רקע שקוף). הכפתור = דלפק קבלה: פינגי + "מתלבטים איפה לגלוש?" + "פינגי כאן, ועונה תוך שנייה", נקודה אדומה בלי מספר שנעלמת בפתיחה (sessionStorage) ומתכווץ לעיגול מתחת ל-560px. הטקסטים ב-`guidance.json → messages_he.launcher_title/launcher_sub/greeting_widget`. האיורים לפי מצב (26/08): `pingi.png` ברירת מחדל · `pingi-ski.png` בדפי יעד · `pingi-board.png` בדפי סנובורד/פארק · `pingi-wave.png` בדף הקייטנות ובפרידה (52px) · `pingi-plain.png` שמור לשימוש עתידי (תומר ביקש 26/08 שגם ליד ההודעות יופיע פינגי עם בגדי החורף, ב-36px)

### שלב C.2 — החדר הנכון בטופס ההזמנה (26/08) — **הושלם, 90/90**

הבעיה: השמות שלנו באים מקובץ ההתחייבויות (`CONN Premium with View 5 pax`) ואלה של
האתר ממנוע ההזמנות (`Premium with View 4-5 pax`, `2 ח"ש וסלון 2-4 אורחים`). בריצה
הראשונה מול האתר החי **אף חדר לא זוהה**. אחרי חמישה סבבים: 90 מתוך 90.

- [x] `server/site-rooms.js` — התאמה על ארבעה צירים בבת אחת:
  **תיאור** (אוצר מילים דו-לשוני: DBL/Double/זוגי, SGL/Single, Dlx/דלוקס, .J/Junior,
  טריפל, סטנדרט, קלאסיק, פריבילג', קומפורט, נישה, One/Two→1/2) ·
  **תפוסה** (`5 pax`, `2-4 אורחים`, וגם טווח חשוף כמו `Premium Room 2-3`; מספר בודד
  סופר חדרי שינה ולא אנשים) ·
  **מטראז'** (`27 mr` ↔ `כ-27 מ"ר` — שתי דירות באותו בניין לא חולקות מטראז', ולכן
  אי-התאמה פוסלת והתאמה יכולה להכריע לבד) ·
  **שם המלון** (siteID 269 עונה גם ל-Sport וגם ל-Strass באותה רשימה)
- [x] שוברי שוויון, בסדר הזה: **כמות האנשים** (פלן סוד מוכר את אותה דירה כ"2-4 אורחים"
  וכ"5 אורחים" — רק הלקוח מפריד ביניהן), ואז **קיבולת** (החדר שהתקרה שלו היא שלנו
  ושמכיל את כל הטווח שלנו). בלי הכרעה — לא נבחר כלום. **לעולם לא מנחשים.**
- [x] `config/booking-url.js → pageFor()` — **קאסה קארינה מוכר שהייה קצרה משבוע בעמוד
  נפרד (siteID 1445)**. אותו חדר, מזהה אחר: `Double Standard room` הוא 3452 שם ו-3395
  בעמוד הרגיל. העמוד הרגיל מחזיר 0 חדרים לחיפוש של 3 לילות. זה המלון היחיד עם עמוד
  כזה (תומר, 26/08), ויש בדיקה שנופלת אם מלון נוסף יקבל אחד בשקט
- [x] `data/catalogue.json` — "Casa Karina Short Stay" הוא עמוד הזמנה ולא מלון 117:
  מסומן `same_as` ומוסתר מכל מה שלקוח קורא, ונשאר נגיש לפי siteID
- [x] `config/room-map.json` — גשר ידני לשמות שלא יתאימו לעולם. כרגע שורה אחת:
  שגיאת כתיב אצלנו ב-Waldheim (`Stillputl` מול `Stilluptal`)
- [x] `public/pingwin-prefill.js` נושא את אותה לוגיקה (אוצר המילים **נוצר** מזה של
  השרת), כדי שקישור שנבנה לפני שהמטמון התחמם עדיין ימלא את החדר בדפדפן. לבד: 84/94
- [x] **אימות** — כי כיסוי הוא לא נכונות. `tests/fixtures/site-rooms-live.json` שומר כל
  חדר שהאתר החזיר ל-40 המלונות; `site-rooms-expected.json` מקבע לכל 90 החדרים את
  המזהה הצפוי (נבדק ידנית אחד-אחד); `test-room-match.js` ו-`test-prefill-match.js`
  עוברים על כל צירוף (עמוד, כמות אנשים) שלקוח יכול להגיע איתו, ומוודאים גם ששום
  תשובה אינה מזהה שהאתר לא החזיר. `npm run match` נותן את הציון אופליין תוך שנייה
- [ ] הקורפוס הוא תצלום מ-26/08. כשפינגווין מוסיפים או משנים חדר — `npm run rooms -- --coverage`, וה-diff ב-expected.json הוא רשימת מה שהשתנה

### שלב C.1 — חוב טכני מסקירת הקוד (26/08) — **1–4 בוצעו**
- [x] **1** לוג השיחות: הבדיקות לא כותבות לתוכו יותר; טלפונים ומיילים מסוננים לפני הכתיבה; מחיקה אוטומטית לפי `CHAT_LOG_DAYS` (30) בעליית השרת
- [x] **2** `faq.json` ו-`deflect.json` נטענים בסלחנות: ערך פגום נזרק לבד, קובץ פגום שומר על הקודם, ועריכה חיה בלי הפעלה מחדש
- [x] **3** שגיאה בניסוח כבר לא מוחקת הצעות: כל שורה, התבנית, המודל וההרכבה עטופים בנפרד
- [x] **4** כל העברית שהלקוח קורא בקונפיג: 18 תשובות ב-`config/deflect.json`, המשפטים הקבועים ב-`guidance.json → messages_he`, והטלפון במקום אחד (`handoff_he.phone`, גם לווידג'ט דרך `/api/config`)
- [ ] 5 מונים בזיכרון בלבד + מונה תורות מהלקוח (רלוונטי כשיש שרת)
- [ ] 6 תקרה על נתונים מהדפדפן
- [ ] 7 קובץ עונה אחד במקום שישה מקומות
- [ ] 8 חותמת Turnstile עם חלון זמן
- [ ] 9 מחיקת `server/prompts.js` (קוד מת שסותר)
- [ ] 10 בדיקות למיזוג ה-LLM ולשליחת הליד

### שלב C.3 — מלאי מתעדכן (27/08) — **הקוד מוכן, נשאר להריץ אצל תומר**

קובץ ההתחייבויות יושב על `\\192.168.0.250\files` (מופיע כ-F: במחשבי המשרד),
בתיקייה `התחייבויות 1\התחייבויות 2027`, בשם `טבלת התחייבויות חדרים חורף 2027.xlsm`.
השרת לא מגיע לשם, ותומר לא רוצה להיכנס למערכות של פינגווין בלי אישור.

- [x] `tools/watch-inventory.js` — **`npm run watch`**. בודק את הקובץ כל דקה;
  ברגע שנשמר, הבוט מתעדכן תוך פחות מדקה. בלי התקנה, בלי הרשאות מנהל, בלי IT.
  מקבל שורש שיתוף ומוצא את הקובץ עד שלוש רמות פנימה — שם הקובץ בעברית
  ומשתנה בין עונות, ונתיב עברי בקונסולה של Windows נשבר בדף הקוד (הכלי מגדיר
  `chcp 65001` בעצמו). מדלג על קובצי הנעילה `~$` של אקסל — הם תמיד החדשים
  ביותר, ובחירה בהם הייתה עוצרת את העדכון בדיוק כשמישהו פותח את הקובץ
- [x] `fs.watch` לא עובד על SMB (אין אירועי שינוי) → בדיקת mtime+גודל. וקובץ
  שנשמר דרך הרשת אינו אטומי → שינוי נלקח רק כששתי בדיקות רצופות מסכימות
- [x] `server/inventory.js` + `POST /api/inventory` — לשרת מרוחק, עם טוקן.
  דוחה: טוקן שגוי · נפילה בשער PII · אובדן יותר מחצי מהמלאי · קובץ ישן יותר ·
  בלי חותמת זמן. כתיבה אטומית, ו-`SkiSearch` טוען מחדש בלי הפעלה מחדש
- [x] `/inventory` — דף שמפענח את האקסל **בתוך הדפדפן** ושולח רק ספירות.
  בלי להתקין כלום. אותם ארבעה מודולים של השרת נטענים לדפדפן דרך `/mod/...`,
  ובדיקה מוודאת שהתוצאה זהה בית-בית לזו של הבנייה
- [x] `data/pii-gate.js` ו-`data/aggregate.js` — קובץ אחד לכל אחד, כי הם רצים
  בשלושה מקומות (בנייה, שרת, דפדפן) וחייבים להסכים
- [x] מלאי מתיישן: אחרי `INVENTORY_STALE_HOURS` (12) הבוט **מפסיק לומר
  "נשאר חדר אחד"** ומוסיף שורה שהזמינות משתנה. ההצעות נשארות
- [ ] **לתומר:** להריץ `node tools/watch-inventory.js "\\192.168.0.250\files\התחייבויות 1\התחייבויות 2027"`
- [x] 07/09: מוזג לענף הראשי יחד עם עבודת 31/08–06/09 (עורך התשובות, תרגום, פורמט כרטיס י). `resort-names.json` הוא הטבלה היחידה לאיות אתרים (טין, לה דוז אלפ)

### שלב D — למידה מתמשכת
- [ ] שגרה שבועית: כישלונות מהלוג → `tests/golden.json` / בנק
- [ ] הידוק מדד הבנק (שאלות שמפעילות חיפוש נספרות כ"התעלמות")

---

## 4. יומן שינויים (מסכם — הפירוט ב-git log)

| תאריך | מה |
|---|---|
| 2026-09-07 | **מיזוג**: עבודת 27/08 (מלאי מתעדכן, `/api/inventory`, דף `/inventory`, טבלת אתרים אחת, רשתות מלונות) מוזגה עם עבודת 31/08–06/09. הווידג'ט נשאר בעיצוב שתומר אישר (סאני, כרטיס י); מהסבב של 27/08 נלקחו טופס הליד עם שגיאות נאמרות, אנימציית כניסה ומצבי לחיצה |
| 2026-08-27 | **סבב UI מול מחקר תעשייה** (4 אג'נטים: Intercom/Zendesk/Crisp/HubSpot/Drift, בוטים של תיירות, קראפט RTL, וביקורת קוד): תוקנו 5 באגים נראים (חותמת זמן בלתי נראית, פוקוס כפול, קפיצת חלון בהקלדה, כותרת טופס לא מעוצבת, משקל פונט לא טעון); מערכת עיצוב — רדיוסים 12→4, פונטים 12→6, בסיסי צל 5→1, hex 33→16, טוקני THEME אמיתיים, `:active` על הכול, טבעת פוקוס אחת ל-16 אלמנטים, אייקוני SVG אחידים; קראפט — אנימציית כניסה 180ms, משמעת גלילה + גלולת "הודעות חדשות", קיבוץ חותמות זמן, טופס ליד בשפת הווידג'ט עם שגיאות אדומות + aria, צ'יפים מיושרים עם fade, `role=log` |
| 2026-08-27 | **מלאי מתעדכן לבד**: `npm run watch` עוקב אחרי קובץ ההתחייבויות ב-F: ומעדכן את הבוט תוך פחות מדקה מכל שמירה. + `/api/inventory` עם שער PII, + דף שמפענח את האקסל בדפדפן בלי להתקין כלום. מלאי ישן מ-12 שעות מפסיק לטעון "נשאר חדר אחד" |
| 2026-08-27 | הכרטיס הוא התמונה של המלון, עם מעבר בין תמונות. טיפוגרפיה מקצועית: קו מתאר סביב הטקסט הוחלף בשכבת הצללה שנושאת את הניגודיות, היררכיה בארבעה גדלים, תגי מחיר ודירוג באשכול בפינה. לחיצה על "פרטים" גוללת את הכרטיס לראש החלון והוא נכנס כולו |
| 2026-08-27 | פחות גלילה: צ'יפים בשורה נגללת (173px → 40px), כרטיס סגור 269px → 213px, חלון רחב כברירת מחדל. מובייל 1.95 → 1.51 מסכים לתשובה |
| 2026-08-27 | "למה אין בפברואר?" עונה את הסיבה (הוואקאנס, פקקים, מחירים) במקום הסבר כללי על מלאי. בדיקה מקבעת את הטענה לנתונים |
| 2026-08-26 | **החדר הנכון בטופס: 34/90 → 90/90 מול האתר החי.** אוצר מילים דו-לשוני, מטראז' כציר הכרעה, כמות האנשים וקיבולת כשוברי שוויון, ושם המלון כשהאתר עונה לשני מלונות ברשימה אחת. כל תשובות האתר שמורות כקורפוס, וכל 90 המזהים מקובעים בבדיקה |
| 2026-08-26 | קאסה קארינה מוכר שהייה קצרה משבוע בעמוד נפרד (1445) עם מזהי חדרים משלו. העמוד נבחר לפי אורך השהייה, והעמוד הזה הוסר מהקטלוג כמלון נפרד |
| 2026-08-26 | שעות פעילות: משפט ההעברה לנציג נקבע לפי השעה בישראל (Asia/Jerusalem, לא שעון השרת) — סגור/פתוח, ומתי נפתח. 13 תשובות FAQ שנגמרו ב"התקשרו" עברו ל-{handoff}. תיקוני דרך: "אפשר לדבר עם נציג?" פותח טופס במקום משפט אוף-טופיק כפול |
| 2026-08-26 | השרת שואל את מנוע ההזמנות של האתר מה מזהי החדרים שלו (`server/site-rooms.js`), כדי שהחדר ייבחר נכון גם כששם החדר אצלנו באנגלית ובאתר בעברית. לא חוסם תשובה, לא מנחש, ונכבה ב-SITE_ROOMS=off |
| 2026-08-26 | "המשך להזמנה" גם מפיק את הצעת המחיר ללקוח (pwquote=1) — רק אחרי שהחדר נבחר ויש לו מחיר. לא נלחץ לעולם "המשך להזמנה" באתר, שמחזיק מסגרת אשראי |
| 2026-08-26 | **"המשך להזמנה" מגיע עם הפרטים מוכנים**: הקישור נושא תאריך, נוסעים, גילאי ילדים, חדר ובסיס אירוח; `public/pingwin-prefill.js` (תג GTM שני) ממלא איתם את טופס ההזמנה באתר |
| 2026-08-26 | מחקר על הבוט של איסתא (4 אג'נטים) → חבילת "ליד לא נאבד": מייל לנציג על כל ליד, מזהה שיחה בכל תשובה, מדידת קליק ההזמנה, וואטסאפ עם כל ההקשר, שדה מייל בטופס. + ליטוש נגישות: יעדי מגע 44px, חותמות זמן, בידוד דו-כיווני, prefers-reduced-motion |
| 2026-08-26 | גילוי נאות בפתיחת השיחה: בינה מלאכותית, ייתכנו אי-דיוקים, נציג מאשר כל הזמנה, וקישור למדיניות הפרטיות (תומר, לפי הבוט של איסתא) |
| 2026-08-26 | תיקוני UI מבדיקת תומר: הכפתור נעלם כשהצ'אט פתוח (פינגי ריחף מעל תיבת ההודעה), פינגי ליד כל תשובה הוגדל ל-36px ולובש בגדי חורף, ומספר טלפון לא נשבר יותר באמצע שורה |
| 2026-08-26 | פינגי מתלבש לפי ההקשר: סקי בדפי יעד, סנובורד בדפי פארק, כדורי שלג בדף הקייטנות, ומנפנף בפרידה. האווטאר הקטן ליד כל תשובה הוא הפינגווין הפשוט — היחיד שנשאר קריא ב-24 פיקסל |
| 2026-08-26 | **פינגי** — לבוט יש דמות ושם. הכפתור הוא דלפק קבלה עם האיור שתומר אישר, נקודה אדומה בלי מספר (נעלמת בפתיחה, חוזרת בביקור הבא), ובמובייל מתכווץ לעיגול. פינגי גם בכותרת החלון, ליד כל תשובה, ובמשפט הפתיחה |
| 2026-08-26 | קטלוג המלונות המלא (117 מלונות ב-23 אתרים, מדפי היעדים ב-pingwin.co.il, בלי קלאב מד ובלי שאלה) — 38 עם התחייבויות ו-79 שנמכרים בכפוף לאישור. הבוט מזהה אותם בשם, נוקב בשמם באתר שאין בו התחייבויות, ולא מציג עליהם תאריך או מחיר |
| 2026-08-26 | תיקון: הצ'יפים עונים על השאלה שעל המסך; "סן אנטון" זוהה (היה מוחלף בשקט ב"אוסטריה") |
| 2026-08-26 | ריצה חיה ראשונה על 100 שאלות של לקוח רגיל: **94%**, 0 מספרים מומצאים, 0 כשלי כלל אדום. ששת הכשלים תוקנו (בית מרקחת, טובוגן, ימי סקי פס, "צרפת יקרה יותר?", ושאלת שתי-מילים שקיבלה בקשת מספר נוסעים במקום תשובה) |
| 2026-08-26 | שאלון תוכן שני: 13 ערכי FAQ חדשים (ציוד נלווה, הלי-סקי/אוף-פיסט, לנגלאוף, סגירת רכבלים, ארוחות ילדים, רפואי, סינגלים, כל זוג, עומס בפברואר, מעיינות חמים, מתקני מלון, סנואו-פארק, חיי הכפר) + פערי ניסוח מהריצה החיה; בנק אופליין 85% → 87%, 0 כשלי כלל אדום |
| 2026-08-26 | גבולות גיל: אין גמישות מתחת ל-4, ומגיל 15 שיעורים באנגלית במקום קייטנה |
| 2026-08-26 | עקבה והמצב הביטחוני: מאמץ ולא הבטחה, בלי ציטוט תקדים 2023 |
| 2026-08-26 | תיקון מבדיקת תומר: "תשווה בין…" לא זוהה כהשוואה, שמות אתרים באנגלית, "על אילו שניים?" בלי כרטיסים, ובלי תג "המשתלם ביותר" בהשוואת אתרים |
| 2026-08-26 | חוב טכני 1–4: לוג נקי ומסונן עם מחיקה אוטומטית, טעינת תוכן סלחנית, ניסוח שלא מוחק הצעות, וכל העברית בקונפיג |
| 2026-08-26 | כפתור "שיחה חדשה" בכותרת; שיחה שמורה נמחקת כשעולה גרסה חדשה; `?pwreset=1`; `npm run test:ui` |
| 2026-08-26 | ווידג'ט: 3 הצעות נכנסות במסך לפטופ בלי גלילה (קיפול פרטים, משפט סוגר מתחת לכרטיסים); ליטוש עיצובי (פונט Assistant, גרדיאנטים, כותרת עם סימן מותג); `npm run dev` (pull + start) |
| 2026-08-26 | עובדות מלון מ-pingwin.co.il (40 מלונות, 15 שדות) → תשובות על ההצעה: בריכה/נוף/מיקום/שיפוץ/מעלית/חדר סקי… |
| 2026-08-26 | שלב B: טבלת אתרים מלאה (מחקר) אושרה לדמו → resort-profiles; מנוע recommend.js; בנק 85% |
| 2026-08-26 | שלב C: limits.js (rate/turns/timeout/budget), Turnstile, origin קשיח, healthz, גרסאות, dataLayer; v0.2.0 |
| 2026-08-26 | טבלת אתרים לאישור (ש' 25); `docs/STATUS.md` נוצר |
| 2026-08-26 | ש' 29–30: צ'קבוקס הסכמה + קישור למדיניות פרטיות בטופס ליד; שורות שפה |
| 2026-08-26 | ש' 13–23: ערבות, תשלומים, שינוי תאריך, טיסות, ק"מ העברות, נציגים, כשרות, ציוד, שיעורים |
| 2026-08-26 | ש' 1–11: קייטנות (לו"ז לכל אתר, גילאים, רמות, צוות), מחירי ילדים, חדרים, תשלום נפרד, ביטול |
| 2026-08-26 | בנק 1,215 שאלות פרסונה + `tests/test-bank.js` (76% → 82%) |
| 2026-08-26 | שומרי התנהגות, כוונות ליד (סוכן/חברה/בית ספר/…), שפות זרות, גבולות גיל, חגים |
| 2026-08-26 | ווידג'ט: מובייל, שימור סשן, WhatsApp, פותחנים לפי עמוד, ליד לא נכשל בשקט |
| 2026-08-25 | golden 42% → 56%, שאלות לפני הצעות, שורת ניגוד בין הצעות (בלי דירוג) |
| 2026-08-24 | מחקר 10 סוכנים (`docs/research/`), תוכנית `improvement-plan.md` |

---

### שתי מדידות שונות של הבנק (26/08)
- **`node tests/test-bank.js`** — כל 1,215 השאלות, אופליין (בלי מודל, בחינם, חוזר על עצמו). זה ה"רצפה" הדטרמיניסטית.
- **`npm run bank:live`** — 100 שאלות שלקוח רגיל שואל, דרך המודל האמיתי, שורה לכל תשובה. הדגל `--typical` מוציא
  ארבעה אשכולות שנועדו להכשיל (אדברסריאלי, חילוץ הבטחות, B2B/אוף-טופיק, שפה ופורמט/רגשי) — 149 שאלות —
  כי ריצה שנועדה לענות על "איך הבוט משרת לקוח רגיל?" לא צריכה להיות מדורגת עליהן. הן עדיין נמדדות בריצה המלאה.
- כל ריצה כותבת קובץ משלה: `bank-results.json` (אופליין), `bank-results-live.json` (150 הקשות),
  `bank-results-live-typical.json` (100 של לקוח רגיל) — כך אף מדידה לא דורסת את השנייה.

## 5. איך מריצים מקומית

```bash
git checkout fix/demo-polish && git pull
npm install            # פעם אחת (אין תלויות חיצוניות כבדות)
npm run dev            # מושך את הגרסה האחרונה מ-GitHub ומפעיל → http://localhost:8787
npm start              # הפעלה בלבד, בלי משיכה (PORT=xxxx לשינוי)
npm test               # כל הבדיקות; שורות "stub exhausted" הן רעש תקין
npm run test:ui        # בדיקות הווידג'ט בדפדפן אמיתי (Playwright)
node tests/test-bank.js
npm run test:prefill   # מילוי טופס ההזמנה בדפדפן אמיתי מול עמוד מדומה
npm run match          # ציון התאמת החדרים אופליין, מול הקורפוס (שנייה)
npm run rooms -- --coverage   # מול האתר החי: 60 בקשות, 2-3 דקות
```
בלי `.env` השרת רץ במצב offline (בלי LLM) — מספיק לדמו ולבדיקות.
`.env.example` מפרט את המפתחות (OPENAI_*, LEAD_WEBHOOK_*, ALLOWED_ORIGINS).

---

## 6. איך אנחנו עובדים (השיטה, כדי שכל סשן ימשיך באותה צורה)

1. **תוכן = קונפיג, לא קוד.** תשובה עסקית חדשה נכנסת ל-`config/faq.json` / `guidance.json` / `departures.json` / `resort-profiles.json` / `data/hotel-facts.json` — עם ציטוט המקור (תומר, שאלון, דף באתר) בהערה.
2. **כל עובדה עם מקור.** מהאתר של פינגווין — ציטוט מילולי. מתומר — "תומר, תאריך, שאלה X". ממחקר אינטרנט — מסומן טיוטה עד אישור.
3. **כל שינוי = בדיקה.** מוסיפים מקרה ב-`tests/` (לרוב `test-end-to-end.js` או קובץ ייעודי) שמצמיד את ההתנהגות; `npm test` חייב להיות ירוק לפני commit.
4. **מודדים עם הבנק.** `node tests/test-bank.js` אחרי כל שלב; הציון מותר לו רק לעלות (82 → 85).
5. **Commit אחד לכל נושא, הודעה מלאה באנגלית** שמסבירה מה ולמה — זה יומן השינויים המפורט (`git log`). `docs/STATUS.md` מסכם, ומתעדכן באותו commit.
6. **הכללים האדומים בודקים אוטומטית**: בנק (מספרים מומצאים), `test-faq.js` (כסף ב-FAQ), `test-recommend.js` (כסף/מלונות/זמנים בהמלצות).

## 7. החלטות שהתקבלו (כדי לא לפתוח מחדש)

- ניסוח דמי ביטול/ערבות: מבנה בלבד / מילים — לא סכומים (כלל 3) עד שתומר יחליט אחרת.
- גיל קייטנה: 4–14; קבוצת 4–6 רק בשבועות שנפתחת; 3 ו-10 חודשים → לא.
- קייטנה: 4–14 בלבד. מתחת ל-4 אין קבוצה ואין גמישות; מגיל 15 שיעורים באנגלית דרכנו, לא קייטנה.
- 14+ חדר משלו ברוב המלונות — הנציג מאשר. מתחת לגיל 14 חדר עם מבוגר.
- ילדים ללא גיל נספרים כנוסעים בכל מקום.
- כל משפט שהלקוח קורא נמצא ב-`config/` — `faq.json`, `deflect.json`, `guidance.json`. הקוד מחזיק את הנוסח כברירת מחדל בלבד.
- הטלפון של המשרד נכתב פעם אחת: `guidance.json → handoff_he.phone`. בקוד יש `{phone}`.
- ניתוח סמנטי: FAQ מסודר לפי סדר — ספציפי לפני כללי (לדוגמה `camp_schedule_tignes` לפני `camp_schedule`).
- מה שאין עליו מידע מאומת (מעיינות חמים, השלגה, מתקני מלון פרטניים) — נאמר במפורש שאין נתון, ומפנים לאתר של פינגווין ולנציג. זה **עדכון** להחלטה מ-24/08 ("הבוט מצטט ולא שולח לאתר"): מצטטים כשיש, מפנים כשאין (תומר, 26/08, שאלה 8).
- הלי-סקי, אוף-פיסט מודרך וקרוס-קאנטרי — לא נמכרים ולא מוצעים דרכנו (תומר, 26/08).
- שלג ורכבלים: אין הבטחה, לא לשלג ולא לפתיחת רכבלים בסופה; כל מקרה נבדק לגופו והחברה עושה כל שביכולתה (תומר, 26/08).
- זוגות חד-מיניים: כל החבילות מתאימות לכל זוג — תשובה קצרה ועניינית, בלי הסתייגויות ובלי הפניה לנציג (תומר, 26/08).
- משפט ההעברה לנציג תלוי שעה (26/08, מעדכן את ההחלטה מ-24/08): בשעות הפעילות "אפשר להתקשר"; מחוצה להן "המשרד סגור עכשיו ונפתח…". ההזמנה להשאיר שם וטלפון נשארת תמיד — רק ה"להתקשר עכשיו" יורד. השעה מחושבת ב-Asia/Jerusalem ולא בשעון השרת (`OFFICE_TZ` לעקיפה).
- מלון מהקטלוג (בלי התחייבויות) — מותר לנקוב בשמו ולהעביר לנציג; אסור להציג עליו תאריך, חדר, מחיר או כרטיס. יש בדיקה שמצמידה את זה (`tests/test-catalogue.js`).
- גילוי נאות (26/08): בפתיחת כל שיחה, בהערת שוליים מתחת לברכה — "השיחה מבוססת בינה מלאכותית — ייתכנו אי-דיוקים, וכל הזמנה מאושרת סופית על ידי נציג" + קישור למדיניות הפרטיות. הנוסח ב-`guidance.json → messages_he.ai_disclosure`, עם `{privacy}` שהופך לקישור. לא נשלח למודל ולא נשמר ביומן — זו הודעת מערכת, לא תור בשיחה.
- git push דרך פרוקסי: `git -c http.proxy= push` (ה-PAT בכתובת ה-remote).

## 8. שיחות נוספות עם סאני (ישרוטל / Abra) — 30/08, ממצאים גולמיים

חמש שיחות, שני מזהי שיחה (`f7a4efd0ae85`, `b73ad7d8b81c`), הכל תועד מילולית.

**מה ראינו בפועל:**

1. **שיחה נשמרת בין ימים.** פתיחת הווידג'ט למחרת שחזרה את השיחה הקודמת עם אותו `מס' מזהה` (`5cbb9cbd0b49`), כולל ההודעה האחרונה. יש כפתור "שיחה חדשה" שמנפיק מזהה חדש. אצלנו: `sessionStorage` — נמחק בסגירת הדפדפן.
2. **המזהה גלוי ללקוח בתחתית הווידג'ט**, קבוע, ליד "שיחה חדשה". אצלנו הוא בכותרת המשנה.
3. **שורת אחזור גלויה לפני כל תשובת ידע**: `בודקת עבורך מידע לגבי: "בריכה מחוממת אילת"`. זו לא שורת המלאי שלנו (`חיפשתי במלאי לפי:`) — זו השאילתה שהבוט בנה לשאלת הידע.
4. **כתיבה מחדש של שאלות אליפטיות.** אחרי "יש בריכה מחוממת?" השאלה "וגם לילדים?" הפכה לשאילתה `"בריכה מחוממת ילדים ים סוף אילת רויאל ביץ אילת"` — כלומר נושא הטור הקודם **+ הישויות שהבוט עצמו הזכיר**, לא רק ההודעה הקודמת של הלקוח.
5. **סולם סלוטים קשיח, ללא הצעות עד שהוא מלא.** אזור+נוסעים → גילאי ילדים → תאריכים. שלוש שאלות בלי שם מלון אחד.
6. **מסמן מה נקלט חלקית**: "יש לי את מספר האורחים באופן חלקי".
7. **מנסח את הסלוט החסר האחרון כפריט בודד**: "חסר לי פרט אחד: כמה מבוגרים יהיו בחדר?" + מה יקרה אחרי.
8. **מלון בשם מפורש + נוסעים + תאריכים → הצעות מיד, אפס שאלות.** ("בראשית לזוג ב-20/11 לשני לילות" → כרטיסים). תואם לכלל `namedPlace → holdLimit = 0` שלנו.
9. **שאלה מרובת-חלקים**: עונה חלקית ומסמנת את מה שאינה יודעת בנימוק מדויק — "אין לי מידע מעודכן על מזג האוויר **שאוכל לאמת כאן**".
10. **"איזה מלון הכי טוב?" → פיצול לפי סגמנט, לא דירוג.** "רויאל ביץ' — יוקרה; המלך שלמה — משפחות; רויאל גארדן — סוויטות". בלי "הכי".
11. **עמידות להזיות**: "ישרוטל כוכב המדבר" (מלון מומצא) → "לא מופיע בין מלונות ישרוטל" + שני המלונות האמיתיים באותו אזור + שאלה סוגרת.
12. **חוסר ידע מנוסח כחוסר בנתון, לא כחוסר במציאות**: "בחדרים שהוצגו לא מופיעה מכונת כביסה ברשימת האבזור, **ולכן אני לא יכולה לאשר** שיש" — ואז **הצעת פעולה קונקרטית**: "אוכל לבדוק מידע ספציפי על שירותי כביסה במלון בראשית. רוצה שאבדוק?" — לא "נציג יבדוק".
13. **נציג אנושי**: נותנת טלפון + קישור מיד, בלי לגייס פרטים, וממשיכה להציע עזרה.
14. **נפילה שקטה**: בשאלה על מלון מומצא + "כמה עולה חניה שם?" — חלק החניה נעלם לגמרי, בלי אמירה.
15. הצעות מוצגות בלי דירוג ובלי השוואה; מלונות מהאקו שלא היו זמינים הושמטו בשקט (8 בשורת האחזור, 6 בתוצאות).

**גבול ההשוואה:** ישרוטל היא בעלת המלונות — סאני נוקבת מחירים וסוגרת אונליין. פינגווין סוגרת בטלפון וכלל 3 אוסר מספרים. לכן מה שמעתיקים הוא **המבנה** (אחזור גלוי → תוצאה → שאלה אחת קדימה), לא המחיר.

## 9. שישה שינויים לפי סאני — מה נבנה, ומה נמדד (30/08)

כל שישה יושמו. `tests/test-sunny.js` (15 בדיקות) מצמיד אותם, וכל שינוי מתועד בקוד עם ההתנהגות שנצפתה אצל סאני.

1. **כתיבה מחדש של שאלה אליפטית** — `offline.isElliptical()` החליף את רשימת הפותחים הסגורה. הכלל החדש: הודעה קצרה היא אליפטית כשכל מילה בה היא מילת תפקוד, פרדיקט (`כלול`, `פנוי`) או מילת רמיזה (`משם`, `אותו`). זה תופס מה שהרשימה פספסה ("ומתי זה?", "רק לכולם?") ושומר על השמירה החשובה — "יש חדר משפחתי?" מכיל שם עצם, ולכן הוא נושא חדש. בנוסף: הישויות שהבוט עצמו הזכיר בטור הקודם (`hotelsNamed` + `canonicalDestination` על תשובת הבוט) נשלחות לנתב יחד עם השאלה, ונושאי-כרטיס נמשכים מהטקסט המשוכתב לתוך `slots.unverifiable`.
2. **`knowledgeEcho()`** — "בודק לגבי: ביטול · Val Thorens" מעל תשובת ידע. `faq()`/`faqMulti()` מחזירים עכשיו `matched` — המילים של הלקוח עצמו שהפעילו את הדפוס, בלי להמציא שם נושא. מוצג רק כשהלקוח לא יכול לדעת אחרת: שאלה שנכתבה מחדש, שאלה שהמודל ניתב, או שאלה שתלויה במלון מסוים. פעם אחת, לא בכל טור.
3. **חוסר בנתון ≠ חוסר במציאות** — `hotel_facility_unknown`, נגישות, עישון ו-`when_unknown_he` נוסחו מחדש ל"לא מופיע במידע שיש לי, ולכן לא אאשר את זה מעצמי" + הצעה קונקרטית לבדוק מלון שיינקב. `defer()` בכרטיסים אומר עכשיו "לא כתוב בדף של המלון אצלי; נציג יאמת".
4. **`offline.blockingGaps()`** — כשנשאר בדיוק פער חוסם אחד, השאלה נעטפת ב"חסר לי פרט אחד: … ואז אביא לכם אפשרויות פנויות." לא על ניסיון חוזר.
5. **שיחה שורדת סגירת דפדפן** — `localStorage` עם `STORE_TTL_MS` (14 יום) ו-`savedAt`, נפילה חזרה ל-`sessionStorage` ואז ל-no-op במצב פרטי. מזהה השיחה נשמר יחד עם הסלוטים, אז שיחה משוחזרת שומרת את המזהה שלה — כמו סאני. הנקודה האדומה על המשגר נשארת per-tab.
6. **תת-שאלה שנפלה** — `droppedQuestionLine()`: הודעה ששאלה שני דברים וקיבלה תשובה אחת אומרת זאת. לא על הודעה שנחסמה בכלל אדום — סירוב מכוון הוא לא שאלה שלא נענתה.

### תיקון לניתוח שנמסר לתומר קודם

אמרתי ש-80 השאלות הקצרות שנפלו בבנק הן בעיקר שאלות אליפטיות ושסעיף 1 יתקן אותן. **זה לא נכון.** ספירה על `bank-results.json`: מתוך 79 הכשלים הקצרים, **3 בלבד** מסווגים אליפטיים. השאר — "אופנועי שלג?", "סנואושו?", "פלייסטיישן?", "שווקים?", "מבטלים?" — יש להם נושא משלהם, והם נופלים כי אין לנו דפוס או תוכן לנושא, לא כי חסר הקשר.

מדידה: `tools/followup-bank.js` נותן 26/79 (33%) לפני השינוי ו-26/79 אחריו — ללא שינוי. על סט של 20 המשכים אליפטיים אמיתיים: 11/20 לפני, 11/20 אחרי (הרגרסיה היחידה, "רק משם?", תוקנה בהוספת מילות הרמיזה). כלומר: **אופליין השינוי הזה כמעט לא זז.** הערך שלו הוא במה שהנתב מקבל (הקשר + ישויות) ובמשיכת נושאי-הכרטיס — ושניהם דורשים מודל חי כדי להימדד. זה מה שיש למדוד ב-`npm run bank:live` אצל תומר.

**מה באמת יסגור את 79 הכשלים הקצרים:** דפוסים ותוכן לנושאים עצמם. חלקם חסומים על תוכן מתומר (אופנועי שלג, סנואושו, שרשראות שלג, שדרוג חדר).

### מדדים אחרי השינוי

- בנק אופליין: **1077/1215 (89%)** — ללא שינוי, וזה צפוי: הבנק שולח כל שאלה כהודעה ראשונה, בלי טור קודם, ולכן הוא עיוור מבנית לסעיפים 1, 2 ו-6.
- **0 מספרים מומצאים, 0 כשלים בכללים אדומים.**
- טסטים דטרמיניסטיים: **688 עוברים, 0 נכשלים** — לראשונה הכל ירוק. שני הכשלים ה"קבועים" (`test-rooms` "not stated", `test-booking-url` "real script") לא היו באגים אלא שני קבצי `tools/` שלא הועתקו לעותק העבודה; הם הועתקו והבדיקות עברו.
- `npm test` כולל עכשיו את `tests/test-sunny.js`.

### נקודה שדורשת החלטה של תומר

שמירת שיחה ב-`localStorage` פירושה שתמליל שיחה שורד סגירת דפדפן על מחשב משפחתי. יש TTL של 14 יום, כפתור "שיחה חדשה" ו-`?pwreset=1`, ונשמרים רק 20 הטורים האחרונים — אבל ההחלטה אם 14 יום זה הנכון היא עסקית. `STORE_TTL_MS` ב-`public/pingwin-bot.js`.

## 10. תיקון ממצאי ביקורת הקוד (30/08)

49 ממצאים מופו, ותוקנו כולם חוץ מאלה שמחכים להחלטה או לתוכן של תומר (סעיף 11).
כל תיקון נעשה בנפרד, עם הרצת טסטים אחריו, ועם בדיקה שמצמידה אותו.

### קריטי

1. **הבטחת קייטנה שקרית** — `server.js:1489` גזר את המשפט "הצגתי רק שבועות שבהם הקייטנה בעברית פועלת" מהדגל ולא מהתוצאה, בעוד המסנן ב-`filter.js` רץ רק כשידועים גילאים. שוחזר: משפחה בלי גילאים קיבלה את בנסקו 4.2 (`no_camp: true`) מתחת למשפט הזה. עכשיו המשפט נגזר מהכרטיסים עצמם, ובלי גילאים נאמר "עוד לא בדקתי קייטנה — בשביל זה אני צריך את הגילאים".
2. **פיצול שני חדרים דילג על מסנן הקייטנה** — `_twoRoomSplits` שכפל ביד 11 תנאים מתוך `_filter` והשמיט את הקייטנה. הוצא `_unitPasses()` משותף; שני המסלולים קוראים לו. הפיצולים גם מקבלים `camps`, `min_adults` ו-`month_alt` שהיו חסרים.
3. **סלוטים מהדפדפן הודפסו מילה במילה** — `server/incoming-slots.js` (חדש): whitelist, אכיפת טיפוסים, וסינון כללים אדומים על הטקסט החופשי. `preferences` הפך ל-enum מהאוצר הסגור של הפרסר. `hotel`/`destination` נפתרים מול הנתונים האמיתיים.
4. **תקציב זמן משותף ל-timeout** — `handleChat` מפרסם את התשובה הדטרמיניסטית שלו לפני קריאת המודל, וה-timeout מחזיר אותה במקום התנצלות. שלוש קריאות המודל חולקות `deadline` אחד במקום 20 שניות כל אחת. נוסח נפרד: "לוקח לי יותר זמן מהרגיל" במקום "אפשר לנסח שוב".
5. **בניית נתונים** — `tools/build-availability.js` (חדש) בונה מחדש את `availability.json` מקובץ ההתחייבויות, עם `--verify` שמשווה בלי לכתוב, ועם בדיקות שמסרבות לכתוב קובץ שגוי. `build-camps.js` ו-`build-restrictions.js` נכתבו כ-placeholders שמסבירים מה חסר במקום לייצר נתונים מנוחשים.

### גבוה

- `phrasing.validateQuestion()` — שאלת ההמשך של מודל הסלוטים עוברת ולידציה (משפט אחד, סימן שאלה, בלי ספרות, בלי שם מלון או אתר).
- `.gitignore` נוצר: `.env*`, `server-data/`, `logs/`, `source-data/`, `node_modules/`.
- `clientIp` סופר מסוף `x-forwarded-for` לפי `TRUST_PROXY` (מספר קפיצות), ותומך ב-`CF_CONNECTING_IP`.
- מונה התורים עבר לצד שרת לפי `_cid`; `_turns` לא מתקבל מהדפדפן.
- `partySize` נמחק וקורא ל-`SkiSearch.partyOf` — סוף להגדרה כפולה.
- `effectiveOcc` — כלל מדף המלון יכול לצמצם, לא להרחיב מעבר לקובץ.
- הקישור להזמנה: הרכב מלא או כלום, ו-`pwquote=1` רק כשידוע מי נוסע.
- הנתב לא שומר במטמון כשלונות, ומפרש JSON בתוך גדרות (`parseModelJSON`).
- טווח מחיר קיים רק למלון מסווג (8 מתוך 40). אין תג "המשתלם ביותר", אין השוואה, ואין "אלה המחירים הטובים ביותר" על מלונות בלי סיווג — במקום זה נאמר במפורש שאין דירוג. `pricing.json → unclassified: "default"` מחזיר את ההתנהגות הקודמת.
- `server/model-health.js` (חדש): מונים לכל שלב, מפסק זרם אחרי כשלים רצופים, ו-`/healthz` מחזיר `degraded` שאפשר להתריע עליו.
- גבול גיל הקייטנה במקום אחד: `camps.json → age_policy.overall` (4–14, החלטת תומר 26/08), דרך `SkiSearch.campAgeRange()`. גיל 6 מתקבל בשתי הקבוצות.
- `server/season.js` (חדש) — שנה, חודשים ותאריכי חגים מ-`data/config/date-labels.json`. `inventory.js:57` כבר לא מקבע 2026/2027.
- הווידג'ט: לא נפתח מעצמו בשחזור, לא מצייר כרטיסים כפולים, לא ננעל על Turnstile תקוע, ולא משכפל הודעה בניסיון חוזר.
- `npm test` רץ על checkout נקי (67 בדיקות במקום קריסה); מספרי הפרסר עברו ל-`tests/workbook-baseline.json`.

### בינוני ונמוך

`server/retention.js` (חדש — מחיקת PII מלידים ישנים) · תמליל הליד עובר `redact` · `count_available` לא נשלח לדפדפן · טעינה מחדש של המלאי בלי restart · צבע תא שלא פוענח → `unknown` ולא `free` · `_shown` בלי כפילויות · ג'יבריש מסלים לנציג בניסיון השני · הודעה ארוכה מדי מקבלת תשובה · חיתוך ל-2000 תווים נאמר ללקוח · טופס ליד בשפת הלקוח · שורת צ'יפים עם דהייה · מרוץ בין "שיחה חדשה" לטור פתוח · `aria-live` על אזור ייעודי · prefill שנכשל אומר זאת · תג ה-GTM השני תוקן · `prompts.js` ו-`prompt-answer.js` נמחקו (בדיקת הספרות שלהם הועברה ל-`prompt-phrase.validate`) · `server/labels.js` (חדש) מחליף 10 עותקים של מפת מדינות · צ'יפים נבחרים לפי `key` ולא לפי regex על העברית של הבוט.

### מדדים

- `npm test`: **24 קבצים, 646 בדיקות, 0 נכשלות** — כולל 6 קבצים חדשים (camps, incoming, slow-model, health, privacy, ובדיקות שנוספו לקבצים קיימים).
- דפדפן: `test-widget` 34 עוברות, `test-prefill` 16 עוברות.
- בנק אופליין: **1077/1215 (89%)** — ללא שינוי, **0 מספרים מומצאים, 0 כשלים בכללים אדומים**.

## 11. מחכה להחלטה או לתוכן של תומר

1. **סיווג מחיר ל-32 מלונות.** כרגע 8 מתוך 40 מסווגים ב-`data/pricing.json`. עד שיסווגו — הם מוצגים בלי טווח מחיר ובלי תג. זה נכון יותר מלהציג ברירת מחדל, אבל פחות שימושי מסיווג אמיתי.
2. **מספר הלילות לכל גיליון** — `data/inventory.js:13-29` מקבע `nights: 7` לעשרה גיליונות תחת הערה "ממתין לאישור תומר". התאריך הזה נכנס לקישור ההזמנה.
3. **`git rm --cached`** — לא הצלחתי להריץ git במחשב שלך. אם `server-data/` או `logs/` כבר במעקב, ה-`.gitignore` החדש לא יסיר אותם: צריך `git rm -r --cached server-data logs` ואז commit.
4. **מקור נתוני הקייטנה וההגבלות** — `build-camps.js` ו-`build-restrictions.js` מחכים לתשובה: מאיפה מגיעים הנתונים, או שהם נשארים ידניים ויורדים מ-`build:data`.
5. **תוכן חסר** (מלפני הביקורת): אופנועי שלג, סנואושו, שרשראות שלג, שדרוג חדר.
6. **איזו מערכת CRM** — `server/crm-lead.js` הוא הקובץ היחיד שישתנה.
7. **תפוגת שיחה של 14 יום** (`STORE_TTL_MS`) — החלטה עסקית, לא טכנית.


## 12. מאגר התשובות — סבב 1 הוטמע (31/08/2026)

תומר עבר על 107 נושאים בדף האישורים והחזיר 106 החלטות: 82 אושרו, 19 בניסוח שלו, 5 נדחו עם הערות.
הוטמעו 101: 70 ערכים חדשים ב-`config/faq.json`, 26 הרחבות match לערכים קיימים, ותיקוני קוד —
recommend כבר לא דורס תשובה ספציפית (`RECOMMEND_MAY_REPLACE` ב-server.js); רשימת מילות התחום של
האוף-טופיק הורחבה (`DOMAIN_HE`); שאלה עם מילת תחום שלא הובנה מקבלת "לא בטוח שהבנתי" ולא "לא התחום
שלי"; שלושה guards חדשים (אישור-לצילום-מסך, ת"ז, הזרקות/ביטקוין) + אסתא ברשימת המתחרים; תעתיק
לטיני, טעויות הקלדה נפוצות (`fixTypos`) ודליפת פריסת מקלדת (`mixed`) מטופלים.

מדידה (אופליין, ללא עלות): בנק הנכונות 68.1% ← **93.5% נכון, 0 שגויות**; בנק ההתנהגות 90%,
0 הפרות חוק קשות; 25/25 קבצי בדיקה, widget 34, prefill 16, selftest 17/17.

פתוח לסבב 2 (דף אינטראקטיבי חדש): biggest_ski_area, resorts_we_dont_sell, my_ski_level,
frequent_flyer, luxury_level (מנוסחים מחדש לפי ההערות), smalltalk_identity (לא הוכרע).
בהמשך: איסוף כוכבים + ציון בוקינג פר מלון מדפי האתר אל data/resorts.json, כדי ש-luxury_level
יוכל לצטט מספרים אמיתיים; רשומת resort-profile לסנט אנטון.

### סבב 2 הוטמע (31/08/2026, מאוחר יותר)

כל 6 הנושאים הפתוחים נסגרו: biggest_ski_area (נוסח תומר, כולל ההערה על סקי פס מורחב בתשלום),
resorts_we_dont_sell (פוצל לפי הנחייתו — תשובה קצרה כברירת מחדל, הרשימה המלאה רק כשמבקשים:
all_resorts_list), my_ski_level (נוסח תומר), frequent_flyer, luxury_level, smalltalk_identity.
בנוסף: **טיניי ← "טין"** בכל פלט ללקוח (faq, resort-profiles, catalogue, prompt-phrase, DEST_HE2)
לפי תיקון של תומר; זיהוי הקלט עדיין מקבל את שתי הצורות. faq.json מונה כעת 209 ערכים.

מדידה אחרי סבב 2: **95.1% נכון, 0 שגויות**, בנק התנהגות 90% / 0 הפרות, 25/25 קבצי בדיקה.
נותר פתוח: איסוף כוכבים + ציון בוקינג פר מלון אל data/resorts.json (כדי ש-luxury_level יצטט
מספרים), ורשומת resort-profile לסנט אנטון ולשאר יעדי הקטלוג.

### דירוגי מלונות נאספו (31/08/2026, ערב)

לפי בקשת תומר, אחרי ריצת אימות מלאה (25/25 קבצים, בנק 90%/0 הפרות, נכונות 95.1%/0 שגויות):
כוכבים וציון אורחים לכל 116 מלונות הקטלוג — קודם מדפי pingwin.co.il (49 כוכבים, 109 ציונים),
והחסר הושלם מ-Booking.com עם אימות שם+אתר לכל מלון. תוצאה: כוכבים ל-104, ציון ל-113.
12 ללא כוכבים (רובם פנסיונים/דירות בלי דירוג רשמי — הושארו null בכוונה), 3 ללא ציון.
נשמר ב-data/catalogue.json (stars, booking_score, rating_source) וב-data/resorts.json לכל 40
המלונות המוצעים; טבלה קריאה ב-docs/hotel-ratings.md. הערה: Belambra Tignes Val Claret רשום
ב-resorts.json עם siteID 1903 אבל דף האתר הוא siteID 2152 — הדירוג הועתק ידנית, שווה ליישר.
עוד לא מחובר לתשובות הבוט — luxury_level מפנה לנתונים; חיבור השוואה חי הוא הצעד הבא אם תרצה.

### הדירוגים חוברו לבוט (31/08/2026, לילה)

- **תשובה דטרמיניסטית חדשה** (`ratingsLine` ב-server.js): "כמה כוכבים יש לרגנום?", "מה
  הדירוג בבוקינג של סטראס ושל בלמברה טין?" — נענים מהמספרים ב-data/resorts.json, זה לצד זה
  ובלי פסיקה מי עדיף. בלי שם מלון — עונה על ההצעות שעל המסך; בלי כלום — התשובה הכללית
  שאושרה (luxury_level). מלון בלי נתון מאומת: "אין לי דירוג מאומת; נציג יבדוק" — לא ניחוש.
- **הכרטיס בווידג'ט** מציג "★ 4 כוכבים · 8.5 בבוקינג" מתחת לשם המלון, רק כשיש נתון.
- **הערך reviews עודכן**: מספרים מאומתים — כן; ציטוט תוכן ביקורות — עדיין לנציג. (הנוסח
  הישן סירב לצטט דירוגים בכלל, מלפני האישור של תומר.)
- כינויים עבריים חדשים לזיהוי מלונות בלמברה וקלאב סוליי לפי יעד (offline-nlu).
- קובץ בדיקות חדש `tests/test-ratings.js` (9 בדיקות) נוסף ל-npm test — כעת 26 קבצים.

אימות מלא אחרי החיבור: 26/26 קבצים, widget 34, בנק 90%/0 הפרות, נכונות 95.1%/0 שגויות.

### הטקסט שנערם — נמצא ותוקן (31/08/2026, לילה)

הבאג מהצילומים של תומר (בועות, צ'יפים ואגודלים מצוירים זה על זה) שוחזר סוף-סוף ונמצא:
`.msgs` היא עמודת flex עם גלילה, וילדי flex מתכווצים כברירת מחדל לפני שהגלילה נכנסת.
ברגע שהשיחה ארוכה מהחלון, הדפדפן כיווץ את בועות הבוט — `min-height:36px` התיר לרדת מתחת
לגובה הטקסט (תשובה של 145px בקופסה של 36px) — והטקסט נצבע מעבר לקופסה על מה שאחריה.
לכן זה הופיע רק בשיחות ארוכות ובשחזור שיחה, ולכן בדיקות שמדדו מלבנים לא תפסו: הקופסאות
לא נחפפות — התוכן גולש מהן. התיקון: `.msgs>*{flex-shrink:0}` (pingwin-bot.js).

שתי השלכות ישרות: (1) שני מבחני רגרסיה חדשים ב-test-widget — אף ילד של .msgs לא מכיל
יותר תוכן מגובה הקופסה שלו, חי וגם אחרי שחזור (36 בדיקות עכשיו). (2) תקציבי הגלילה מ-26/08
כוילו מחדש (1.6 לפטופ / 2.15 מובייל): המדידות המקוריות נלקחו כשהבאג כיווץ את הבועות, כלומר
חלק מה"קומפקטיות" ההיא היה טקסט על טקסט. בנוסף תוקנה שעה לבנה-על-בהיר בבועת הלקוח.

### מתקני מלון — נאספו וחוברו (31/08/2026, לילה)

צעד 2 מתוכנית "להיות כמו סאני": עומק ידע פר מלון. נאספו 17 שדות מתקנים לכל 116 מלונות
הקטלוג מדפי pingwin.co.il (בריכה + מחוממת/מקורה, ספא, חדר כושר, חדר סקי, מעלית, חניה,
אינטרנט, מטבחון/מקרר, חדר משחקים, הסעדה, מרפסת, כביסה, מרחק מהמעלית, שאטל, מיקום) —
בניסוח הדף עצמו, ללא מחירים (סינון בשלב האיסוף + חגורת FACT_MONEY בזמן מענה), ערך חסר =
הדף לא אומר. ממוצע ~9 שדות למלון. נשמר ב-resorts.json (page_facts, הקיים מ-26/08 גובר)
וב-catalogue.json (facts, 114 מלונות; 2 דפי 404).

חוברה תשובה דטרמיניסטית `facilityLine` (server.js): "יש בריכה מחוממת בסטראס?" → ציטוט
הדף + "האם מחוממת לא כתוב — נציג יאמת"; "יש ספא במלונות האלה?" אחרי הצעות → שלוש שורות
מהדפים. גוברת על התשובות הגנריות (spa/wifi/parking/hotel_facility_unknown/lockers/
slope_distance) ועל חטיפת הכשרות של "מקרר" — אבל שאלת כשרות אמיתית נשארת אצל kosher.
כשאין אף עובדה — המסלול הקיים ("לא מצאתי בדפי המלונות אצלי") עונה.

`tests/test-facilities.js` (10 בדיקות) נוסף ל-npm test — 27 קבצים. אימות מלא: 27/27,
widget 36, בנק 90%/0 הפרות, נכונות 95.1%/0 שגויות. סאני, להזכיר, נכשלה על "בריכה מחוממת".

### סבב איכות התשובות (31/08/2026, לילה) — ראו docs/answer-quality.md

כל 1,215 השאלות נשפטו על איכות התשובה, לא רק על נכונות. עיקרי התיקונים: שאלות
שנבלעו בזמן מילוי פרטים 37→3 (שורת הודאה + ~35 דפוסים); recommend כבר לא עונה
על "שוברים"/"מדברים"/"חברה" (מלכודת \b עברית, מופע 6); lifts_hours,
organized_tour, missed_flight-extend; זיכוי סקיפס ללא-גולשים בתשובת הסקי-פס;
"יש בריכה מחוממת?" כללית נענית מהנתונים. בנק התנהגות 90%→92%, נכונות 95.1%/0.

### שש התשובות של תומר נכנסו + מחקר פעוטות (31/08/2026, לילה מאוחר)

תומר ענה על ששת פערי התוכן: (1) ימי הקייטנה — ערך camp_days חדש מתוך טבלת "חופשות
שבועיות" שצילם (שישה ימי הדרכה, שישי עד 11:45, חצי יום אחרון גלישת משפחות; בקצרות 3–4
ימים); (2) ילד לבד במעלית — child_alone_lift (משתנה לפי אתר, נציג יבדוק); (3) בוסטר —
נוסף ל-transfer_vehicle (מתאמים מול נציג); (4) החלפת כסף — currency_exchange (בארץ /
שדה / נציג ביעד); (5) סקיפס יומי/חצי יום — נוסף לתשובת הסקי-פס ("אנחנו כחברה לא מוכרים
אותם"); (6) פעוטון פר אתר — נחקר לפי בקשתו: 16 אתרים מול האתרים הרשמיים/בתי הספר לסקי,
נשמר ב-resort-profiles.json (toddler, כולל source ו-winter_verified; מונז'נבר ועוז ממקור
עמוד קיץ — מסומנים לאימות), ותשובת toddler_snow_play שוכתבה מהנתונים. פאס דה לה קאסה —
אין מעון (הקרוב בגראו רואיג); בנסקו ובורובץ — לא נמצאה מסגרת.

מדידה: בנק התנהגות 92% (1117/1215), נכונות 95.2%/0 שגויות, 27/27 קבצים, widget 36.

### עיצוב הווידג'ט — שפת סאני, מיתוג פינגווין (31/08/2026)

תומר: "שיהיה כמו סאני רק שיישאר מיתוג של פינגי". פתחתי את סאני בדפדפן ומדדתי את
ה-CSS החי שלה (isrotel.co.il), ואת צבעי המותג משכתי מהאתר החי של פינגווין — הערכה
שהייתה בקוד עד היום הייתה placeholder עם TODO.

**מסאני (מדוד, לא משוער):** שני הצדדים הם אותו כרטיס — רוחב מלא, פינות 8px, ריפוד
14px, צל `0 2px 6px 1px rgba(0,0,0,.05)`; רק הרקע מבדיל (לבן מול תכלת #DDE9FC).
מרווח צפוף (~10px), כותרת 17-18/700 בצבע המותג עם תפקיד 12px, שדה קלט לבן 8px עם
מסגרת בגוון בועת הלקוח, ופס תחתון עם מס' השיחה מול "שיחה חדשה".

**מפינגווין (מהאתר, לא placeholder):** #37455C כחול-צפחה, #F0674C אלמוג, Open Sans
Hebrew. הפינגווין נשאר — אבל מופיע רק בראש רצף (`.m.bot.lead`), כך שרצף שורות מפינגי
נראה כתשובה אחת ולא כשלוש.

עוד: השעה ירדה משורה משלה לפינת הכרטיס; כרטיסי ההצעות יושרו לאותה שפה (8px + אותו
צל) במקום 16px וצל אחר; כפתור השליחה 40px/8px במקום ריבוע גרדיאנט 44px; ירדה טבעת
פוקוס שלישית מיותרת. הפס התחתון לקח 26px מאזור השיחה, אז המרווח מעל כרטיס שנפתח
הפך לאדפטיבי (`Math.min(12, pane - card)`) במקום קבוע — נכון לכל גודל חלון.

אימות: 27/27 קבצים, widget 36, prefill 16, נכונות 95.2%/0 שגויות. גיבוי לפני השינוי:
/tmp/bot-before-redesign.js (בקונטיינר בלבד).

### מבחן הכללה על שאלות חדשות (31/08/2026) — ראו docs/generalisation-test.md

269 שאלות חדשות (אפס חפיפה עם הבנק) + 25 שיחות רב-תוריות. הכללה לשאלות בודדות:
64% (מול 95% על הבנק המכוונן — הפער הוא מדד השינון). שיחות: 52%→88% אחרי תיקונים.
אפס עובדות מומצאות גם מחוץ למדגם. תוקנו 6 באגים שבנק חד-תורי לא יכול למצוא, ובראשם
"אני מעדיף לדבר עם בנאדם" שקיבל "זה לא התחום שלי" (הדפוס הכיר רק "בן אדם" בשתי מילים).
פתוחות: 2 חשיפות אבטחה שממתינות להחלטה (bug-bounty fishing, בקשת תרגום להונאת גיל).

### קישור ההזמנה היה שבור — תוקן (31/08/2026)

תומר הדביק את סקריפט ה-prefill בקונסול ולא קרה כלום. בדיקה על הדף החי חשפה
שהקישור שהבוט מייצר נחת על **לשונית התיאור** של דף המלון, לא על לשונית ההזמנה:
אין `#step1`, אין `#roomsBlock` ואין `window.orderMan` — הסקריפט חיכה 15 שניות
למשהו שלא קיים וויתר בשקט (כמתוכנן: הוא לעולם לא שובר את הדף).

הסיבה: חסר `&tab=20`. ההערה בראש `config/booking-url.js` ידעה את זה מ-26/08
("The hotel page's quote section is ?siteID=<id>&tab=20") — הקוד פשוט לא הוסיף
אותו, ושתי בדיקות ב-`test-booking-url` קיבעו את הכתובת השגויה והצמידו את הבאג.

תוקן ב-`buildBookingUrl`, והבדיקות עודכנו לאמת החדשה (כולל טענה מפורשת
ש-`tab=20` קיים). אומת על הדף החי: התאריכים נכנסו, רשימת החדרים נטענה, וחדר
3721 שהבוט מבקש נמצא בה ("2 ח״ש וסלון 5 אורחים" — מתאים ל-5 הנוסעים).

**זה השפיע על כל קישור "המשך להזמנה" שהבוט הציג אי פעם.**

### מספר הטלפון נצבע הפוך — תוקן (31/08/2026)

תומר צילם: הבוט הציג `8557722-04` במקום `04-8557722`. הסיבה היא תיקון קודם שלי —
כדי שהמספר לא יישבר בין שתי שורות הוחלף המקף הרגיל במקף לא-שביר (U+2011). אבל
U+2011 הוא תו **נייטרלי** באלגוריתם הדו-כיווניות של יוניקוד, בעוד המקף הרגיל
(U+002D) הוא מפריד-מספרים שמחבר שתי קבוצות ספרות לרצף אחד. הנייטרלי הפריד את
"04" מ-"8557722", ובפסקה עברית שתי הקבוצות נצבעו בסדר ימין-לשמאל.

תוקן: מקף רגיל עטוף בבידוד LTR (U+2066…U+2069) — הכיוון מובטח גם בקצה שורה.
נמדד בדפדפן על שש חלופות לפני הבחירה, ונוספה בדיקת רגרסיה (test-widget, 37
בדיקות) שמודדת את מיקום ה-x של כל תו — כלומר את מה שהעין רואה, לא את המחרוזת.

### 30 שיחות פרסונה (31/08/2026) — ראו docs/persona-test.md

30 סוגי לקוחות, 151 תורות, כל שיחה נקראה ונשפטה ידנית. **2 טובות, 8 חלקיות,
20 נשברו.** הפער מ-95% בבנק הוא מבני: הבנק בודק הודעה ראשונה בשיחה ריקה, ושלושת
השוברים המרכזיים מופיעים רק כשיש כרטיסים על המסך והקשר קודם.

השוברים: (1) "אני כאן בעיקר להתאמת חופשות סקי" — 18 תורות ב-15 שיחות, כולל לקוח
עם מוגבלות ושאלת "למה שאזמין אצלכם"; (2) שורת הסגירה בולעת את השאלה — 11 שיחות;
(3) שכבת הפרטיות יורה על ההזמנה של הלקוח עצמו, ובכיוון ההפוך לא נורית על בקשת
פרטי לקוח אחר כשיש כרטיסים; (4) לולאות שפה (תעתיק ורוסית) שמהן אין יציאה.
בנוסף: הודעה מלאה וברורה ("4 נפשות, ילדים 6 ו-8, פברואר, בולגריה") נדחתה.

הסטים נשמרו: tests/persona-bank.json, והתמלילים המלאים ב-docs/persona-transcripts.md.

## 03/09 — ריצת פרסונות לייב (OpenAI)
- `npm run personas:live` הורץ על המחשב של תומר; 151 תורות, 104 דרך המודל, ~1$.
- תוצאה: 10 טובות / 12 חלקיות / 8 שבורות (אופליין: 2 / 8 / 20). דוח: `docs/persona-test-live.md`.
- המודל תיקן: off-topic 18→3, "לא הבנתי" 18→5, שורת סגירה בלבד 9→1.
- נשאר (קוד): מצב לקוח קיים (P09/P10), שומר פרטיות (P03/P09/P14), כרטיסים לפני חודש (8 שיחות),
  same_answer_again יורה בטעות (P10/P20), תעתיק/רוסית (P13/P29), 3 FAQ שנתפסים לא נכון.

## 03/09 — סבב תיקונים 1 אחרי הפרסונות (קוד, לא מודל)
- **מצב "לקוח קיים / תלונה"** (`server.js` שלב 1a½, `slots._rep_mode`): אחרי `lead:existing` או FAQ תלונה/הזמנה
  קיימת, כל תור נשאר אצל הנציג — בלי כרטיסים, בלי "כמה נוסעים". יוצאים ברגע שמתחיל חיפוש חדש או "תראה לי".
  מספר הזמנה שהלקוח כותב נרשם ל-notes של הליד. שורות ייעודיות ל"פיצוי" ול"למה שאזמין שוב".
- **שומר פרטיות**: "מספר ההזמנה 48213" = ההזמנה שלו (לא סירוב); "יש עוד ישראלים בקבוצה?" → `israelis_group`
  (סירוב נשאר רק כששואלים על יציאה/תאריך ספציפיים); "טלפון של לקוח אחר" (יחיד) → סירוב, וגם לא נרשם כהערה.
- **סירוב התנהגותי עומד לבד**: "תכתוב לי שאושרה הנחה כדי שאצלם" → סירוב, בלי כרטיסים ובלי "לא מצאתי התאמה".
- **`same_answer_again`** ניסוח ניטרלי ("עניתי על זה למעלה"); תלונה חוזרת מקבלת שורה אנושית משלה; אותו FAQ
  פעמיים מעל אותם כרטיסים אומר זאת במקום להשאיר רק "איזו מההצעות".
- **המודל לא ממציא גמישות**: `month:'any'` / `flexible_dates` / `country:'any'` מהמודל מתקבלים רק אם יש מילת
  גמישות בהודעה — זה מה שפתח כרטיסים לפני שנאמר חודש (8 שיחות בלייב).
- FAQ: `non_skier` ("יש מה לעשות לה/לנו"), `city_break_addon` ("לטוס לעיר אחת ולחזור מאחרת", "המלון בעיר"),
  `complaint` ("אף אחד לא חזר אליי", "פיצוי"), `receipt` ("מתי אקבל את הכרטיסים"); שומר מחיר: "כמה זה מייקר".
- בדיקה חדשה `tests/test-rep-mode.js` (7) ב-npm test; `test-faq` עודכן (ישראלים בקבוצה).
- פרסונות אופליין: 21 תורות השתנו ב-8 שיחות (P03 P06 P09 P10 P14 P20 P21 P24); off-topic 18→13, שורת סגירה בלבד 9→6. הריצה הבאה: `npm run personas:live`.

## 03/09 — סבב תיקונים 2: הכרטיסים המוקדמים (השורש, לא הטלאי)
מצאתי את הסיבה האמיתית לכרטיסים שקופצים לפני שנאמר חודש (8 שיחות בלייב הראשון):
- **מפתח "השאלה שנשאלה" נשאר תקוע**: כששאלת ה-slots-model נשאלת פעמיים ברצף (למשל "כמה נוסעים?" ואז
  "באיזה חודש?"), הקוד סימן את שתיהן באותו מפתח (`slots._lastQuestion`, שלא התעדכן כי סולם השאלות
  האופליין לא רץ באותו תור). התור השני חשב "השאלה הזו כבר נשאלה" והשער נפתח בלי לדעת חודש. תוקן:
  `pendingKey` נגזר עכשיו תמיד מהשאלה שהמודל שאל *בתור הזה*, לא מהזיכרון הישן. שיחזרתי את הבאג בסימולציה
  עם מודל מדומה (סקריפט קבוע מראש, בלי לשלם) ואימתתי את התיקון על אותה סימולציה.
- **"4 נפשות" / "ילדים 6 ו-8"**: שני דפוסי ניסוח שלא פוענחו נכון —
  "4 נפשות" בלי הזכרת ילדים = משפחה ישראלית טיפוסית 2+2 (כמו "משפחה של 4"), לא ארבעה מבוגרים;
  "ילדים 6 ו-8" (בלי המילה "בני") לא זוהה כלל כגילאים. שניהם היו הגורם שהמודל קיבל sluts חלקיים
  ושאל שוב את מה שהלקוח כבר אמר (P28 "רק מספר המבוגרים" אחרי "4 נפשות, ילדים 6 ו-8, פברואר, בולגריה").
- בדיקות אופליין: 5 תורות נוספות השתנו (P05 P20 P22 P26 P28), ובדיקת P01 מראה שהכרטיסים הראשונים עכשיו
  מופיעים בתור 4 (אחרי מבוגרים+ילדים+חודש) במקום בתור 2 (אחרי מבוגרים+ילדים בלבד).
- כל 28 קובצי הבדיקה + `test-rep-mode.js` עדיין ירוקים.
- **חשוב**: זה עדיין לא נבדק בלייב מול OpenAI — הריצה הבאה (`npm run personas:live`) היא האימות האמיתי,
  כי הבאג המקורי נגרם רק כשהמודל האמיתי שואל שתי שאלות ברצף בניסוח משתנה.

## 03/09 — סבב תיקונים 3: עוד שלוש נקודות מהריצה השנייה
- **"היא/הוא לא תגלוש"** — בלי מילת ילד בקרבת מקום זו שאלה על בן/בת זוג (`non_skier`), לא על ילד
  (`non_skiing_child`); עם מילת ילד ליד (עד ~25 תווים) זה נשאר על הילד. תוקן ב-`config/faq.json`.
- **"כמה?" לבד** — תמיד שאלת מחיר בדומיין הזה; עכשיו נתפס במפורש בלי לפגוע ב"כמה אנשים בחדר?" וכדומה.
- **"אפשר גם בעברית"** אחרי תור בשפה זרה — זיהוי כאישור להמשיך בעברית, לא שאלה שמנותבת למודל
  (שהיה בוחר `hebrew_staff` ומחזיר פסקה על מלווים ומדריכים). עכשיו ממשיך ישר לשאלה הבאה.
- 3 בדיקות חדשות נוספו ל-`tests/test-rep-mode.js` (עכשיו 10 בסך הכול). כל 28 קובצי הבדיקה ירוקים.
- פרסונות אופליין: 3 תורות השתנו בדיוק בשלוש השיחות הרלוונטיות (P12, P18, P29) — שום דבר אחר לא זז.
- **נשאר פתוח, לא טופל** (עומק/סיכון גבוה יחסית לתועלת, או תלוי בך): P02 "אפשר לראות אופציות?" ו-P08
  "תשאירו לי מחיר ליומיים?" (ניסוח מעורפל, המודל בוחר יעד/פרשנות לא מדויקים); P22 "יש להם מסגרת?"
  (תלוי בהקשר משני תורות אחורה — המנתב לא מקבל את ההקשר הזה כרגע); P13/P29 תעתיק לטיני ורוסית עדיין
  חוזרים על אותה שורה בלי לנסות להבין; P20 "למה שאזמין דווקא אצלכם?" — עדיין אין תשובה, צריך ניסוח ממך.

## 03/09 — סבב תיקונים 4: "תקציר משפחה" למנתב (אופציה 2 שאישרת)
בעיית ה-P22 ("יש להם מסגרת?" אחרי שהגילאים נאמרו שני תורות אחורה) היא ארכיטקטונית: המנתב הסמנטי
(`answer-router.js`) מקבל רק את המשפט הנוכחי, ומנגנון ה"תור קצר שממשיך משפט קודם" (`isElliptical`) לא
תופס משפטים עם מילות תוכן אמיתיות כמו "מסגרת"/"מבוגרים". הפתרון שבחרת (אופציה 2 מתוך 4):
- **`partyDigest(slots)`** (`server.js`) — פונקציה חדשה שבונה שורת רקע קצרה מתוך ה-slots שכבר נאספו:
  מספר מבוגרים, גילאי הילדים/נערים (ממוינים), האם רוצים קייטנה בעברית, היעד, עד 4 העדפות ועד 3 הערות
  אחרונות של הלקוח. לדוגמה: `רקע על הנוסעים: 2 מבוגרים | ילדים/נערים בני 14, 16 | בלי קייטנה | יעד: austria`.
- שני מקומות הקריאה ל-`routeToAnswer` (השאלה הבודדת, ושאלות מרובות-חלקים) עכשיו עוטפים את הטקסט
  הנשלח למנתב ב-`רקע על הנוסעים: ...\nשאלה: <מה שהלקוח כתב>` — בכל תור, לא רק בתורים אליפטיים קצרים.
  זה חל תמיד, לא רק כשיש חשד להקשר חסר, כי אין עלות בלתי-הפיכה: אם השאלה עומדת בפני עצמה המנתב
  פשוט מתעלם מהרקע.
- קאש התשובות (`ROUTE_CACHE`) ממשיך להיות ממופתח לפי הטקסט המדויק שנשלח (כולל הרקע) — כמו שכבר
  נהוג עם שמות מלונות (`withEntities`) — כדי שלא יתערבבו הקשרים של לקוחות שונים.
- **תוך כדי בדיקה נמצא ותוקן באג נפרד ב-`offline-nlu.js`**: המשפט "...בני 14 ו-16, 2 מבוגרים..." גרם
  ל-2 (ממספר המבוגרים) להיספח בטעות לרשימת גילאי הילדים (`children_ages: [14, 16, 2]`), כי מנגנון פענוח
  הגילאים סורק כל המספרים בקטע הרציף שמתחיל ב"בני" ונעצר רק על שם חודש. תוקן: מספר שמופיע ממש
  לפני המילה "מבוגרים" נגרע מהקטע לפני חיפוש הגילאים. נבדק גם על "ילדים בני 4, 6, 9, 12" (רשימת ילדים
  אמיתית עם פסיקים) ועל "משפחה עם 2 מבוגרים וילדים בני 4 ו-6" — שניהם עדיין נכונים.
- אומת בסימולציה עם מודל מדומה (בלי לשלם): המנתב מקבל עכשיו את הרקע המלא והנקי לפני השאלה.
- כל 28+ קובצי הבדיקה (כולל `test-rep-mode.js`, 10 בדיקות) ירוקים; פרסונות אופליין — **אפס** תורות השתנו
  (צפוי: המנתב הסמנטי לא רץ כלל במצב אופליין, אז השינוי לא אמור להשפיע על תוצאות האופליין — רק על
  איך שהמנתב האמיתי מול OpenAI יתנהג).
- **חשוב**: כמו סבב 2, זה תלוי במודל האמיתי ולא ניתן לאימות מלא באופליין. השאלה האמיתית — האם P22
  עכשיו עונה נכון — דורשת ריצת `npm run personas:live` נוספת.

## 06/09 — ריצת לייב שלישית (OpenAI) + סבב תיקונים 5
**מה הריצה הראתה** (`tests/persona-results-live.json`, 151 תורות, 96 דרך המודל):
- ✅ אומת בלייב: מצב לקוח קיים/תלונה (P09, P10 — כל התורות אצל הנציג, יציאה נקייה לחיפוש חדש);
  סירובים עומדים לבד (P14); "כמה?" = שאלת מחיר (P12); "היא לא תגלוש, רק אני" → בן/בת זוג (P18);
  "אפשר גם בעברית" → ממשיך בעברית וגם משלים את השיחה (P29 עכשיו שיחה טובה); "4 נפשות, ילדים 6 ו-8"
  מפוענח נכון (P28); "יש עוד ישראלים בקבוצה?" נענה (P03). off-topic 3, "לא הבנתי" 3, שורת סגירה בלבד 1.
- ❌ **הכרטיסים המוקדמים לא נפתרו** — 13 שיחות עדיין קיבלו כרטיסים לפני שנאמר חודש. הפעם, עם שדה
  ה-`gate` בתוצאות, נמצא **השורש האמיתי** (סבב 2 תיקן מקרה אחר, אמיתי אבל לא זה):
  הבוט שואל "באיזה חודש?", הלקוח עונה בשאלה משלו ("יש עוד ישראלים בקבוצה?"), המודל רוצה לשאול שוב
  את אותה שאלה — וכלל "לא שואלים פעמיים" השמיט אותה. בלי שאלה שתרכב על התשובה, `holdingForDetails`
  נהיה false והשער נפתח (P03, P04, P06, P17, P21, P30, וגם P12 בסולם האופליין).
- ❌ P22 "יש להם מסגרת או שהם עם המבוגרים?" — המנתב קיבל את התקציר ("נערים בני 14, 16") ובכל זאת בחר
  `organized_tour` (בגלל המילה "מסגרת"). P16 (24 עובדים) נפל מהליד הקבוצתי ל"נוסעים גם ילדים?" ואז
  כרטיסים. P18 "רק אני" נקרא כנוסע יחיד ואז "זוג" לא דרס — הוצעו חדרים ליחיד לזוג. P25 "זוג, ינואר, מה יש?"
  → "אין לי תשובה מוכנה". P11 תורות 4-5: המחשב נרדם באמצע (timeout של 3 שעות) — לא באג בבוט.

**סבב 5 (קוד, לא מודל):**
- **שאלה שנענתה בשאלה נשאלת פעם נוספת — והשער ממשיך להחזיק** (`server.js`, ליד `tailQuestion`):
  מפתח שכבר נשאל מותר פעם אחת נוספת כל עוד חסר פרט מהותי, `_held` < 3 ועדיין לא הוצגו כרטיסים.
  אם המודל חזר על אותו ניסוח מילה במילה — מוסיפים מסגרת "עוד פרט שחסר לי כדי לדייק: ..."
  (`guidance.json → ask_again`), כך ש"לא אותה שאלה פעמיים" נשמר ובדיקת ה-e2e הקיימת עוברת. אחרי
  שתי התעלמויות השאלה נזנחת והדלת נפתחת — אף אחד לא תקוע. בנוסף: הסולם האופליין לא אומר יותר
  "סליחה, לא הצלחתי להבין" ללקוח שאמר "פברואר" (הבנו אותו — הוא פשוט ענה על משהו אחר).
- **קבוצות נשארות אצל נציג הקבוצות** (`_rep_mode: 'group'`): אחרי `lead:corporate/school/celebration_group`
  כל תור נשאר אצל הנציג עם `group_followup`/`group_tail`; הטופס נשאר מתויג `corporate`. יציאה כרגיל
  ("תראה לי", חיפוש חדש). `receipt` לא בולע יותר "חשבונית על שם החברה" (→ `company_invoice`).
- **המנתב מבין את התקציר**: הפרומפט ב-`answer-router.js` מסביר מה זו השורה "רקע על הנוסעים" ואיך
  להשתמש בה (כולל הדוגמה של P22). ל-FAQ נוסף שדה אופציונלי `topic_he` שמחליף את תיאור התבנית
  האוטומטי כשהוא יוצא ג'יבריש ("בנןת 13-7") — הוגדר ל-`teen_camp`.
- **"רק אני" + דיבור על גלישה** לא קובע נוסע יחיד; **"זוג"** לבדו דורס ספירה קודמת ("חדר זוגי" עדיין רק ממלא חסר).
- **"מה יש?"** בסוף פרטים = בקשה לראות. **"לנסוע/נסיעה/לטוס"** נוספו ל-`DOMAIN_HE` (P23 "אפשר בכלל לנסוע
  איתם?" לא נאמר לו יותר "לא התחום שלי").
- `tests/test-rep-mode.js` — 7 בדיקות חדשות (17 בסך הכול). כל 28 קובצי הבדיקה ירוקים.
- פרסונות אופליין: 67 תורות השתנו — כמעט כולן "הכרטיסים הראשונים מופיעים תור אחד מאוחר יותר, אחרי
  שאלה חוזרת"; P16 בלי כרטיסים בכלל (נכון ל-24 עובדים); P18 שני מבוגרים; P12 מגיע לכרטיסים עם
  יעד ידוע.

**עדיין פתוח / תלוי בך:** P20 "למה שאזמין דווקא אצלכם?" ו-P23 "אפשר בכלל לנסוע עם תינוק?" — אין
תשובה מאושרת ב-FAQ, צריך ניסוח ממך. P13 (תעתיק לטיני) ו-P29 ברוסית עד המעבר לעברית — לולאת שפה, לא
טופל. P02 "אפשר לראות אופציות?" אחרי שהכרטיסים כבר על המסך — עמימות אמיתית. P15 "לא יודע מתי הם
יכולים" → נשאל "מתי?" מיד אחר כך (רעיון: לקרוא "לא יודע מתי" כ"גמיש"). P22 — האם התיקון במנתב עובד
יודעים רק בלייב.

## 06/09 — שתי תשובות תוכן מתומר (סבב 5, המשך)
- **`travel_with_baby`** (חדש ב-`config/faq.json`) — "אפשר בכלל לנסוע עם תינוק?": אפשר; פעוטון במועדוני
  קלאב דו סוליי; מיטת תינוק כבקשה מהמלון; הכול באישור המלון/המועדון, נציג מאשר לפני הסגירה.
  התבנית תופסת רק *שאלות* ("אפשר/כדאי לנסוע עם תינוק"), לא הצהרות ("יש לנו תינוק בן שנה") — אלה
  ממשיכות לשאלת המבוגרים. "אפשר בכלל לנסוע איתם?" בלי מילת תינוק מגיע למנתב עם התקציר (גילאים 1, 3)
  ו-`topic_he` שמתאר את התשובה.
  ⚠ לתשומת לבך: התשובה הקיימת `non_skiing_child` אומרת "פעוטון בקלאב דו סוליי **ובלמברה**"; בהודעה
  שלך הזכרת רק קלאב דו סוליי. השארתי את שתיהן כמו שהן — תגיד אם בלמברה צריך לרדת.
- **"למה שאזמין דווקא אצלכם?"** — לפי הנחייתך ("לא יכול להמליץ או להשוות") מנותב לתשובת `competitor`
  הקיימת ("לא אשווה מול חברות אחרות... מה שאני כן יכול לומר זה מה כלול אצלנו"). נוספו גם "למה דווקא
  אתם", "למה לבחור בכם", "יש לכם משהו שאין להם", "מה היתרון שלכם".
- בדיקה ב-`test-rep-mode.js` (18). כל 28 הקבצים ירוקים. פרסונות אופליין: רק P20 השתנתה (4 תורות).

## 06/09 — בנק שאלות נפוצות כשרשורים (218 שרשורים, 654 תורות)
- `tests/faq-threads.json` — 218 שרשורים ב-21 נושאים (הזמנה, תשלום, ביטול, טיסות, הסעות, מלון, אוכל, שבת,
  קייטנה, ילדים, נוער, תינוקות, מתחילים, ציוד, סקי פס, ביטוח, שלג, יעדים, מחיר, קבוצות, נגישות, מסמכים,
  שירות). כל שרשור: שאלה, ואחרי התשובה עד שתי שאלות המשך שנבנות עליה ("וזה כלול?", "וכמה זה עולה?").
- `tests/test-faq-threads.js` — מריץ אותם מול השרת (אופליין/`--live`), כותב `tests/faq-threads-results[-live].json`
  וגם **תמליל קריא** `docs/faq-threads-transcript[-live].md` (שאלה/תשובה עם סימוני 🃏 כרטיסים, 📋 טופס,
  ⚠ "לא הבנתי"/"מחוץ לתחום"/"אין תשובה מוכנה"). מסננים: `--only F001,F050`, `--topic קייטנה`, `--first 20`.
  npm: `npm run faq` / `npm run faq:live`.
- אופליין (בלי מודל): 257/654 תורות בלי תשובה — צפוי, שאלות ההמשך ("וזה כלול?") תלויות במנתב שרץ רק בלייב.
  שאלות ראשונות: 48/218 בלי תשובה באופליין — אלה פערי regex/תוכן שאפשר לסגור גם בלי מודל.
- תוך כדי: הסולם לא משתמש יותר ב"סליחה, לא הצלחתי להבין" לשאלה שהלקוח לא ראה (FAQ-only מסמן 'adults'
  בלי לשאול); המסגרת "עוד פרט שחסר לי" עוטפת את הניסוח הרגיל ולא את ניסוח ה-retry; "מקום" ב-`DOMAIN_HE`.

## 06/09 — שיחה שלישית עם סאני + ארבעה באגים שנחשפו בהשוואה
מסמך מלא: `docs/sunny-vs-pingi-06-09.md` (תמליל סאני, אותה שיחה מול פינגי, 7 פערים לפי חשיבות,
המלצה: "עורך תשובה" אחד שכותב תשובה מהחומר המאושר במקום להדביק שורות; אפיון מותנה של מלונות —
דורש אישור מדיניות; תלונה רכה; פתיחה עם שלוש שאלות).
תוקן היום מתוך ההשוואה (הכול דטרמיניסטי, `tests/test-rep-mode.js` 20 בדיקות, 28 קבצים ירוקים):
- `tipping` תפס "טיפל" (מלכודת המחרוזת העברית) — עכשיו "טיפ" עם גבולות מילה.
- "לפני שנתיים" הפך לילד בן 2 ודרס גילאים ידועים — "שנתיים" נספר כגיל רק ליד מילת ילד/בן/בת.
- תלונה באמצע חיפוש: אין יותר "ברוכים השבים!" מעל "מצטער לשמוע", ואין כרטיסים ולא הערות חיפוש
  מתחת לתלונה (כמו סאני: התנצלות, ערוץ, ורק אז המשך).
- `complaint` תופס גם "היה מלוכלך", "אף אחד לא טיפל", "יחס גרוע"; `competitor` תופס "למה דווקא
  דרככם / ולא בבוקינג / באתר אחר יותר זול" ומשתיק את `country_price`; `ski_school` תופס
  "יש קייטנה לילדים?", "מי שומר עליהם?".

## 06/09 — ריצת שאלות נפוצות בלייב (654 תורות) + סבב תיקונים 6 + שתי מדיניות חדשות
ניתוח מלא: `docs/faq-live-analysis-06-09.md`. תמצית: שאלה ראשונה 97% נענית; שאלות המשך — 41% בלי
תשובה, בשלושה דפוסים דטרמיניסטיים שתוקנו:
- **`offline.pickSentence(question, answer)`** — המשך שנופל על אותה תשובה מצטט את המשפט הרלוונטי
  מתוכה ("אפשר בביט?" → "ביט ופייפאל — לא.") במקום "עניתי על זה למעלה" (79 תורות). מופעל בשני
  מסלולי החזרה (FAQ-only ו-dedupe מעל כרטיסים), וגם **לפני המנתב**: המשך של עד 4 מילים שמתחיל ב-ו'
  מחפש קודם משפט בתשובה הקודמת ("ולצרפת?" → שורת צרפת) — בלי מודל.
- **`followup_unknown`** (guidance) — שאלה קצרה אחרי תשובת ידע שלא הובנה מקבלת "על הפרט הזה אין לי
  תשובה מאושרת — נציג ישלים" (❔ בתמליל) במקום "לא התחום שלי"/"לא הבנתי" (87 תורות). מתכון לעוגה
  עדיין "מחוץ לתחום".
- **המנתב מקבל את ההודעה הקודמת** (+מזהה התשובה) להודעות קצרות, עם הנחיה שהמשך קצר ממשיך את הנושא.
- **מדיניות תומר (06/09)**: (1) **פתיחה עם שלוש שאלות במשפט** — `opening_question` בסולם (רק תור ראשון,
  רק פערים פתוחים, ≥2), `greeting` עודכן, `ask_never_he` מתיר למודל אותו דבר; (2) **אפיון מלונות** —
  `server/characterize.js` (תגיות + כוכבים + בוקינג + עובדות דף → פרופיל לכל מלון + "אם חשוב לכם X — A")
  מחליף את `compare` ("לא אדרג"); בלי מחירים, בלי "הכי". `hotelsNamed` מזהה מילה ייחודית משם מלון
  ("Ferienhof", "Kristal"); `catalogueHotelLine` לא יורה על שם שמתאים למלון שבמלאי ("Sport" ≠ MPM Sport).
- `test-rep-mode.js` 25 · `test-end-to-end` "naming two hotels" עודכן למדיניות · 28 קבצים ירוקים.
- אופליין: FAQ-threads 257→124 בלי תשובה; פרסונות — כרטיסים ראשונים תור אחד מאוחר יותר (השאלה
  הכפולה בפתיחה). הקבצים: `tests/faq-threads-results[-live].json`, `docs/faq-threads-transcript*.md`.
- (המשך 06/09) שש מתוך שבע השאלות הראשונות שנכשלו בלייב נתפסות עכשיו ב-regex: "איך האוכל בצרפת" → `food_by_country`,
  "הקייטנה פועלת בכל שבוע / מובטחת / לא יהיו מספיק ילדים" → `camp_price`, "מגבלות רפואיות / לחץ דם / אחרי ניתוח"
  → `health_rules`, "בולגריה שווה?" → `compare_countries`. נשארו כפערי תוכן: "מה זה בלמברה?" ו"אפשר להחליף מלון אחרי שהזמנתי?".

## 06/09 — עורך התשובה (סעיף 3.8 מהסיכום; "תעשה את 3.8 של הכתיבה")
- **`server/compose.js`** — פרומפט "אתה עורך" + `validate()`. המודל מקבל: הודעת הלקוח, תקציר המשפחה,
  ההודעה הקודמת של הבוט, **כל השורות שהמערכת הכינה** (ה"חומר"), והשאלה שהתור חייב להסתיים בה — וכותב
  הודעה אחת (2–5 משפטים). מותר לסדר, למזג, לקצר ולהשמיט תבניות; אסור להוסיף עובדה.
- **השומר** דוחה: מספר / מילה לטינית / שם מלון או אתר שלא בחומר; סימן כסף; "הכי טוב"; אימוג'י; רשימה; שאלה
  שנשמטה או הוחלפה; יותר סימני שאלה מבחומר; טלפון שנשמט כשמדברים על להתקשר; סירוב שרוכך; ופלט שרוב
  מילותיו לא מהחומר (< 72% כיסוי גזעים). עריכה שנדחתה → השורות המקוריות. תקלה במודל → השורות המקוריות.
- **איפה רץ**: תורים **בלי כרטיסים** (ידע, המשכים, אפיון, "אין לי תשובה מאושרת") — במסלול ה-FAQ המוקדם
  ובמסלול הראשי. **לא רץ** על: סירובים (guard), תלונה, קליק על צ'יפ / מספר / "כן", תור בלי תשובת ידע שיש
  בו פחות מ-3 שורות, תורים עם כרטיסים (שם נשאר `phraseWithModel`). `REPLY_EDITOR=off` מכבה.
- עלות: קריאת מודל אחת נוספת לתור ידע (~2–4 שניות). שלב `compose` נוסף ל-`model-health` (/healthz).
- `tests/test-compose.js` (13): השומר על עריכות טובות/רעות, החיווט (מסלולים שכן/לא עורכים, דגל כיבוי).
  `test-orchestration` סופר עכשיו קריאות עורך בנפרד ומוודא שצ'יפים לא משלמים. 29 קבצים ירוקים.
- **לא מאומת בלייב** — רק בסימולציה עם מודל מדומה. הריצה הבאה (`npm run faq:live`) היא הבדיקה האמיתית:
  לקרוא את התמליל ולראות אם התשובות נקראות כפסקה אחת, ואם השומר דוחה יותר מדי (בלוג: "reply editor rejected").

## 06/09 — בנק עשן (20 שיחות) + שאלון תוכן 3 כדף אינטראקטיבי
- `tests/smoke-threads.json` — 20 שיחות ממוקדות, אחת לכל תיקון מהיום (פתיחה, שער, המשכים, הקשר, תלונה,
  קבוצות, אפיון, פענוח, שפות, עורך). לכל שיחה שדה `checks` שמופיע בתמליל — מה בודקים. `npm run smoke` /
  `npm run smoke:live`. הרץ קיבל `--bank <קובץ>` וקורא לפלטים לפי שם הבנק (`tests/smoke-threads-results[-live].json`,
  `docs/smoke-threads-transcript[-live].md`, `tests/smoke-threads-editor-live.log`).
- שאלון תוכן 3: `docs/content-questionnaire-3.docx` (51 שאלות) **וגם** דף אינטראקטיבי (Artifact ב-claude.ai, קובץ
  המקור `docs/questionnaire-3.html`) — לכל שאלה הצעת תשובה שלי, ותומר מאשר / משכתב / מדלג; כל לחיצה נשמרת
  ב-db של ה-Artifact ואני קורא משם. ההצעות הן ניחוש מושכל — כלום לא נכנס ל-FAQ בלי אישור.

## 06/09 — ריצת עשן לייב (`npm run smoke:live`) ומה תוקן אחריה
- תוצאה: 20 שיחות, 53 תורות, ⚠ 0, ❔ 3. 12 שיחות עברו כפי שנבדק (S01–S08, S10, S13, S16, S18). 8 דרשו תיקון —
  כולם תוקנו דטרמיניסטית, עם בדיקה לכל אחד ב-`tests/test-rep-mode.js` (34):
  - S09 מתבגרים 14 ו-16 + "יש להם מסגרת?" → הראוטר בחר "טיול מאורגן". עכשיו: גילאי 13–17 בסלוטים + מסגרת/קייטנה/הדרכה
    → `teen_camp` בלי ראוטר. אותו דבר לתינוק: גיל ≤3 + "אפשר בכלל לנסוע איתם?" → `travel_with_baby`.
  - S11 "למה דווקא אצלכם?" אחרי תשובת ההשוואה → "עניתי על זה למעלה". עכשיו (`repeatAnswer`): אם אין משפט לצטט
    ותשובה קצרה (≤260 תווים) — חוזרים עליה עם "כאמור —". רק פסקה ארוכה מקבלת "זה כל מה שיש לי".
  - S12 "בוא נראה בכל זאת מה יש" אחרי תלונה → "אין לי תשובה מאושרת" מעל הכרטיסים. `wantsToSee` הועלה לפני
    ההכרעה על off-topic, קיבל "בוא נראה / מה יש" חשוף, ומוציא מ-`knowledgeFollowup`.
  - S14 "ואיזה מהם?" אחרי השוואת Sport/Ferienhof אפיין את הכרטיסים שעל המסך. עכשיו `slots._compared` זוכר את הזוג
    שהושווה, והוא גובר על הכרטיסים (`characterize.js`, `incoming-slots.js`).
  - S15 מעל פרופיל Regnum: "בודק לגבי: Regnum" ו"רשמתי גם: מתעניין ב-Regnum" — שניהם הוסרו (אין הד מעל תשובת
    מלון-ספציפית; אין הד כשהמילים של הלקוח עצמן התאימו ל-FAQ; הערת מלון שזה עתה אופיין נמחקת).
  - S17 "זוג, ינואר, מה יש?" שאל יעד ולא הציג. עכשיו: בקשה לראות + הרכב + חודש ידועים → כרטיסים, שאלת היעד מתחתם.
  - S19 אחרי רוסית → "אפשר גם בעברית" שאל רק "כמה נוסעים?". עכשיו זו הפתיחה עם שלוש השאלות.
  - S20 "סיננתי יציאות בשבת — כל מה שמוצג…" כשעוד לא הוצג כלום → "אסנן… אציג רק…" בזמן שהכרטיסים מוחזקים.
- אופליין: `shabbat_flights` תופס גם "טיסות שלא בשבת", `pregnancy` תופס "אשתי בחודש חמישי". `npm run smoke` אופליין: ⚠ 0.
- **כיול שומר העורך** (`server/compose.js`) לפי `tests/smoke-threads-editor-live.log` — 9 דחיות, 6 מהן על עריכות
  נאמנות: סף הכיסוי 72% → 60%, ושורש משותף (3 אותיות) נספר ככיסוי ("נשלחים"/"יישלחו"); "קצר מדי" רק מתחת ל-25 תווים
  (37 תווים נכונים נזרקו); שאלת סיום בשני סימני שאלה ("באיזה חודש? דצמבר…גמיש?") היא שאלה אחת — אבל
  "רוצים בשקלים? כמה תהיו?" עדיין שתיים; "תתקשרו" נחשב דיבור על טלפון. כל 9 העריכות שנדחו עוברות עכשיו את השומר;
  פסקה מומצאת עדיין נדחית (18%–38%). `tests/test-compose.js`: 13 ירוקים.
- **הבא**: `npm run smoke:live` שוב — לראות שהעורך עכשיו מנסח את S20 כפסקה אחת, ושהראוטר לא נדרש ב-S09/S10.

## 06/09 — ריצת עשן לייב שנייה (אחרי התיקונים)
- 53 תורות, 39 דרך המודל, ⚠ 0, ❔ 2, עורך: 3 דחיות (במקום 9). **19/20 עברו**: S09 עונה teen_camp בלי ראוטר,
  S10 תינוק, S11 "כאמור —", S12 כרטיסים בלי "אין לי תשובה מאושרת", S15 פרופיל Regnum כפסקה אחת (העורך!),
  S17 כרטיסים + שאלת יעד, S19 שלוש שאלות "באוסטריה", S20 שבת כפסקה אחת ו"אסנן…".
- נשאר ותוקן: **S14 תור 3** — כשהאפיון בא עם כרטיסים ושינוי סלוט (העדפות ספא/שקט), מודל הניסוח של הכרטיסים
  סיפר מחדש את ההשוואה ("בריכה רק ב-Berghof" מתחת לשורה שאומרת ש-Sport עם בריכה) וה-dedupe מחק את שורת Ferienhof.
  עכשיו `hotel_characterization`/`hotel_facility_facts`/`hotel_ratings` ב-`NO_PARAGRAPH_AFTER` — הפרופיל שלם, מתחתיו
  שורת התבנית של הכרטיסים, בלי פסקה של המודל. בדיקה ב-`tests/test-compose.js` (14).
- שומר העורך: "refusal softened" נדחה בטעות על "אין לי **כרגע** תשובה מאושרת" — ה-fallback `/(?:לא|אין)\b/` לא עבד
  אף פעם (‎\b‎ לא עובד ליד עברית). עכשיו לכל סירוב רשימת ניסוחים שמשמרים אותו; "היתרון הוא שקיפות…" (S11) עדיין נדחה.
- S19 תור 3 הציג כרטיסים בלי לשאול על קייטנה — נכון: היעד אוסטריה נקלט מהרוסית, ובפברואר אין שם קייטנה לגיל 10,
  אז השאלה מיותרת והבוט אמר זאת ("אין שבוע שבו פועלת קייטנה בעברית לגילאים שלכם").

## 06/09 (ערב) — סאני מול פינגי, בטבלה
- תומר: "תחקור עוד על סאני ותשווה בין פינגי אליה בטבלה מסודרת". מקורות ציבוריים (IAS, אייס, abra): הושקה 15/04/2026,
  נבנתה עם אברא (Azure OpenAI + מאגר ידע לפי אברא), ~1M ₪, "נציגת ה-AI הראשונה בתיירות בישראל". שיחה שלישית איתה
  (מזהה db312b85dfdd): שתי שאלות בהודעה, "יקר לי", שינוי הרכב, ביטול, בוט?, מתכון, אנגלית, ג'יבריש, נציג, "ובאילת?", "יש עוד".
  אותה שיחה הורצה על פינגי (אופליין) — `docs/sunny-vs-pingi-table.html` (גם Artifact): 28 שורות, סאני 4 · פינגי 7 ·
  שווה/מדיניות 15 · תוקן היום 2. הפערים שנשארו: תשובה כפסקה גם בתורות עם כרטיסים (12), תוכן באנגלית/רוסית (21),
  ג'יבריש אחרי כרטיסים (22), ספציפיות במדיניות ביטול (16 — החלטה שלך).
- מה סאני עשתה פחות טוב: החניה לא נענתה ב-10 תורות, "3 מבוגרים" → "תקלה טכנית", שאלה שוב "כמה ילדים", "אני צריך" בפרסונה נקבה.
- תוקן מההרצה של המראה: `WANTS_MORE` תופס "יש עוד אפשרויות/הצעות/מלונות", "תציג עוד"; "בן אדם / בן זוג / בת דודה" לא נספרים
  כילד (`offline-nlu.js`, שים לב ל-ן סופית ב-`[ןנת]`); שורת "עדכנתי — 3 מבוגרים — וחיפשתי מחדש:" (guidance `search_updated`)
  כשהרכב/גילאים/חודש/יעד משתנים אחרי שהוצגו כרטיסים; "ואגב" נחשב שאלה שנייה. `tests/test-rep-mode.js`: 37.

## 06/09 (ערב) — העורך גם בתורות עם כרטיסים
- תומר, אחרי שראה זו לצד זו את "זוג, ינואר" שלנו (פסקת מודל + "חסר לי פרט אחד:" + שורת סגירה) מול סאני: "כן תעשה את זה".
- `server/server.js`: `composeWithModel` רץ עכשיו גם כשיש כרטיסים (אחרי מודל הניסוח — שתי קריאות בתור כזה, אותו deadline).
  לא רץ על: סירוב התנהגותי, תלונה, guard בלי כרטיסים, ופרופיל מלון (`hotel_characterization`/`facility_facts`/`ratings` —
  שלמים כפי שהם). כלל המחיר ("המחיר המדויק… במסך ההזמנה") כן נערך — עם כרטיסים, כי הוא נענה איתם בכוונה.
- העורך מקבל `כרטיסים_מוצגים_מתחת` (שמות המלונות) — מותר לו להתייחס אליהם ("האפשרויות שלמטה"), ואסור מלון אחר; השמות
  נספרים כחומר לשומר. `server/compose.js` — כלל 8 בפרומפט, ושומרי עובדות לתורות כרטיסים: כלל המחיר, טענת הקייטנה
  ("הצגתי רק שבועות…", "אין שבוע שבו…"), סינון שבת, שורת ההזמנה, פיצול לשני חדרים — חייבים לשרוד במשמעותם.
- `tests/test-compose.js` (15): תור כרטיסים עובר דרך העורך עם השמות; עריכה נאמנה עוברת; השמטת טענת הקייטנה נדחית;
  מלון שלא בכרטיסים נדחה. הריצה הבאה: `npm run smoke:live` — S01/S17/S18 הם התורות שבהם רואים את ההבדל.
- ריצה שלישית של `smoke:live` (אחרי העורך-על-כרטיסים): 43 תורות דרך המודל, ⚠ 0, 2 דחיות. כל תורות הכרטיסים יצאו כפסקה אחת
  ומסתיימות בשאלה אחת (S01, S03, S11, S12, S16, S17, S18); העובדות מהכרטיסים ("הכל כלול וספא" ב-Belambra) אומתו מול resorts.json.
  דחייה אחת נכונה (S11 ספין), אחת מיותרת — "אין לי כרגע עלות מאושרת" — נוספו עלות/מחיר/נתון/פרט לניסוחים המותרים.
  `docs/sunny-vs-pingi-table.html` עודכן: שורה 12 (כתיבה כפסקה) עברה מ"סאני" ל"תוקן היום"; סאני 3 · פינגי 7.

## 06/09 (לילה) — ג'יבריש + תוכן באנגלית/רוסית (שתי השורות שנשארו לסאני)
- **ג'יבריש**: `offline.isGibberish` — ריצה על שורת מקלדת (עברית/לטינית, 4 תווים), אות חוזרת ×3, וכלום מהותי מלבד זה.
  צחוק ("חחח"/"הההה") ומספרים אינם ג'יבריש. התשובה: "נראה שההודעה נשלחה בטעות — אם יש שאלה, כתבו אותה ואענה"
  (guidance `sent_by_mistake`) — לפני כרטיסים במקום "לא בטוח שהבנתי", ואחרי כרטיסים כשורה מעליהם. "asdfgh" כבר לא אנגלית.
- **שפות** (`server/translate.js`, עטיפה ב-`handleChat`): הודעה באנגלית/רוסית/צרפתית/ערבית מתורגמת לעברית (קריאת מודל, JSON),
  התור רץ כרגיל בעברית, והתשובה מתורגמת חזרה (קריאה שנייה). שומרים: בכניסה — הכל עברית, כל המספרים של הלקוח נשמרים,
  שם מלון מהמלאי נשמר; ביציאה — אף אות עברית, אף מספר שלא היה במקור, ≥70% מהמספרים נשמרים (הטלפון תמיד), מילים
  לטיניות (שמות מלונות) נשמרות, אורך סביר, בלי אימוג'י/רשימות. כישלון בכל אחד → המשפט הקבוע בשפה + טופס, כמו קודם.
  `slots._lang` דביק: "4" או "ok" עדיין חוזרים באנגלית; הודעה עם עברית מסיימת את התרגום. `TRANSLATE=off` מכבה.
  אופליין (בלי מפתח) — כמו קודם. הכרטיסים והצ'יפים נשארים בעברית בינתיים. שלב 'translate' ב-model-health.
- תוך כדי: "אנחנו 2 מבוגרים וילד בן 7" נתן מבוגר אחד ("אנחנו 2" נקרא כסה"כ) — תוקן ב-`offline-nlu.js`.
- `tests/test-translate.js` (10) בשרשרת; `tests/smoke-threads.json` קיבל S21 (אנגלית), S22 (רוסית), S23 (ג'יבריש) — 23 שיחות.
  **לאמת בלייב**: `npm run smoke:live` — לקרוא את S21/S22 בתמליל ולוודא שהתשובות באנגלית/רוסית ולא המשפט הקבוע.
- ריצת לייב רביעית: S21/S22 ענו ב-8–26ms — התרגום לא רץ כלל. הסיבה: deadline של `25000/3` (לא שלם) → הטיימר של הלקוח
  זרק "delay is out of range" → נפילה שקטה למשפט הקבוע. תוקן (`Math.round`); הסטאב בבדיקה בודק עכשיו שה-deadline שלם.
  הרץ מסמן 🌐 תשובה בלי עברית ואוסף גם `translate-in/out rejected|failed` ללוג. **להריץ שוב `npm run smoke:live`.**
- ריצת לייב חמישית — התרגום עובד: S21 באנגלית (הקייטנה, הפרידה, הטלפון), S22 ברוסית, S19 הרוסי הראשון עונה ברוסית.
  שני פספוסים שנתפסו ותוקנו בשומר + בפרומפט: "Penguin" במקום Pingwin (מותג חייב להישאר), ו"лагерь на русском языке"
  במקום "קייטנה בעברית" (המילה "עברית" חייבת לצאת כ-Hebrew/иврит/hébreu/العبرية). "יש חבילות לאוסטריה?" נחשב בקשה
  לראות (קיבל "אין לי תשובה מוכנה"). S23 ג'יבריש: העורך שילב את השורה יפה. 2 דחיות עורך, שתיהן נכונות.
  הטבלה מול סאני: שורות 21 ו-22 → "תוקן היום"; נשארה שורה אחת אצל סאני (16, מדיניות ביטול עם מספרים — החלטה שלך).

## 06/09 (לילה) — שתי הצעות במקום שלוש, וסקין בסגנון סאני
- תומר: "הבוט יראה 2 אופציות במקום 3 ואם הלקוח יבקש 3 זה יגדיל לו את החלון". `CARDS_DEFAULT` (ברירת מחדל 2): החיפוש
  עדיין בוחר שלוש; `cards` = השתיים הראשונות, `spare_cards` = השלישית. הטקסט (ניסוח/עורך) מדבר על מה שמוצג. `_shown`
  זוכר רק את המוצג — הווידג'ט מוסיף את הרזרבה ל-`_shown` כשהוא חושף אותה, כך ש"יש עוד?" לא חוזר עליה.
- ווידג'ט: כפתור "+ עוד אפשרות" מתחת לשני הכרטיסים חושף את השלישי מיד (בלי סיבוב לשרת), מרחיב את החלון (`.win.wide`,
  1100px כמו שהיה), רושם ביומן ובהודעות (`[הוצגו עוד 1 הצעות: …]`). החלון הרגיל עם הצעות: 760px.
- סקין קרוב יותר לסאני (מהצילומים של הווידג'ט שלה): כותרת בפס תכלת עם דיוקן עגול, "פינגי · פינגווין / נציג דיגיטלי";
  גוף לבן, בועת בוט אפור-תכלת בלי מסגרת, בועת לקוח תכלת; כרטיס לבן עם תמונה למעלה (118px), כפתור "המשך להזמנה" כחול
  ברוחב מלא ו"תחזרו אליי" כקישור (ברירת המחדל עכשיו; `?pwcard=photo` מחזיר את כרטיס-התמונה); שדה קלט מעוגל עם כפתור
  שליחה עגול מחוצה לו; "התשובות בגדר המלצה — כל הזמנה מאושרת סופית על ידי נציג" מעל שדה הקלט.
- בדיקות שהניחו 3 כרטיסים עודכנו ל-2 + רזרבה (`test-offline`, `test-orchestration`). `npm start` → localhost:8787 לראות.
- הערות תומר על הסקין (06/09 לילה): (1) אימוג'י הזכוכית המגדלת מעל "חיפשתי במלאי לפי" → אייקון קו דק ב-SVG.
  (2) "צריך לגלול הרבה": השעה מתחת לכל הודעה בוטלה (חסכה שורה להודעה) — במקומה חותמת זמן מרכזית רק כשהשיחה
  עצרה לחצי שעה או עברה יום; ריווח העמודה 7px, בועות 10/14px, תמונת כרטיס 96px; כפתור "↓ להודעה האחרונה" צף
  כשגוללים למעלה יותר מ-240px. (3) העיגול האדום על פינגי — נשלחו 6 חלופות (`fab-options.png`), ממתין לבחירה.
- הכפתור (06/09, תומר): העיגול ירוק (#2fb26a) ומהבהב כמו קודם; ריחוף עם העכבר מחליף את פינגי בתמונת הבורד
  (`pingi-board.png`, מוצמדת מראש ב-DOM ונכנסת ב-fade; גם בפוקוס מקלדת). בדפי הבורד, שם הבורד הוא כבר תמונת הבסיס, אין החלפה.
- הנסיעה (06/09, תומר): בפתיחה הראשונה בביקור פינגי על הבורד גולש מהכפתור באלכסון עד הדיוקן בכותרת (~0.9 שניות,
  Web Animations, קשת קלה עם הטיה), החלון נפרש מלמטה למעלה מאחוריו (clip-path), ובנחיתה הדיוקן קופץ קפיצה קטנה.
  פעם אחת ל-session (`pw_rode`), לא ב-prefers-reduced-motion, `?pwreset=1` מאפס גם אותה.
- תומר (צילום): החלון ברוחב המלא (1100px) כבר עם שתי הצעות — לא רק אחרי "עוד אפשרות"; והתמונות ב-16:9 (עד 210px גובה)
  במקום פס של 96px. עם שלוש הכרטיסים מצטופפים באותו חלון.
- "למה פינגי נפתח על כל המסך?": `fitWidth()` הוסיף `.big` (1100px × גובה המסך) בכל פתיחה במסך ≥1180px — מ-26/08.
  עכשיו החלון נפתח קטן (460px) וגדל רק כשמגיעות הצעות (או בלחיצה על ⤡); הקלדה כבר לא מרחיבה; מצב "מקסימום" לא נשמר
  בין טעינות (`pingwin_bot_max` נמחק). כותרת המשנה קוצרה ל"נציג דיגיטלי · זמינות מהמלאי שלנו" והפלייסהולדר ל"אפשר לכתוב כאן…".
- "פרטים" (06/09): הלחיצה על הקישור הקטן בקצה הכרטיס פספסה (נפתח "צור קשר" של הדף מתחת). עכשיו שורה ברוחב הכרטיס,
  40px גובה, עם קו מעליה, "עוד פרטים ▾" / "פחות פרטים ▴", `type=button` + עצירת האירוע; כפתורי הכרטיס 40px מינימום;
  ריפוד תחתון לאזור ההודעות כך שהשורה האחרונה לא מתחבאת מאחורי שדה הקלט.
- "looks messy" (06/09, מסך נמוך): התמונה 210px בלעה את הכרטיס וכפתור "↓ להודעה האחרונה" ישב על שם המלון. עכשיו גובה
  התמונה `clamp(100px, 24vh, 210px)` ובמסך נמוך מ-640px — 110px; כפתור הקפיצה מופיע רק אחרי שהלקוח גלל בעצמו
  (גלגלת/מגע/מקלדת), לא אחרי שמיקמנו את התצוגה בכוונה על ההצעות.
- פורמט כרטיס אחיד (06/09, תומר בחר "י" מתוך 10 אופציות): פס כחול (גרדיאנט המותג) מתחת לתמונה עם שם המלון, אתר · מדינה
  ותג דירוג "★ 4 · 8.7 בבוקינג"; ארבע קוביות — יציאה · לילות · חדר · מחיר; עד 4 תגיות (תגיות המלון מהדפים — ספא, קרוב
  למסלולים… — עכשיו מוצגות בכרטיס הסגור, עד 3, + "נשאר חדר אחד"/"מומלץ"); שורת "מתאים ל-N נוסעים"; כפתור; "תחזרו אליי";
  "עוד פרטים". `.card.j` — רק על הכרטיס הלבן; כרטיס-התמונה (`?pwcard=photo`) לא השתנה. הכרטיס הפתוח כמו קודם.
- "עוד פרטים" (06/09): (1) שורת המתג ישבה באמצע הכרטיס הפתוח עם שוליים שליליים וחפפה את "החבילה כוללת" — עכשיו היא
  תמיד השורה האחרונה (order 20) והכפתורים יורדים מתחת לפרטים כשהכרטיס פתוח. (2) פתיחה הקפיצה את הכרטיס לראש החלון —
  עכשיו הגלילה היא רק כמה שצריך כדי שהחלק שנפתח ייראה, ואף פעם לא מעבר לראש הכרטיס.
