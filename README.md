# 🐧 בוט התאמת חופשות סקי — פינגווין (דמו)

צ'אט צף בעברית לאתר פינגווין: הלקוח כותב בשפה חופשית, הבוט משלים מה שחסר
בשאלה-שתיים לכל היותר, ומציג **3 חבילות אמיתיות שפנויות בפועל** מתוך קובץ
ההתחייבויות של חורף 2026/27.

## הרצה מקומית

```bash
node server/server.js
```

ולגלוש אל: **http://localhost:8787**

זהו. בלי התקנות, בלי תלויות — Node בלבד.

### שלושה מצבי עבודה

| מצב | מה צריך | איך זה מבין עברית |
|---|---|---|
| **דמו (ברירת מחדל)** | כלום. עובד עכשיו, בחינם | מנתח דטרמיניסטי (מספרים, גילאים, חודשים, יעדים, שדות תעופה) |
| **OpenAI** | `OPENAI_API_KEY` ב-`.env` | `gpt-5.6-luna` — הזול, מספיק לגמרי למשימה |
| **Claude** | `ANTHROPIC_API_KEY` ב-`.env` | דרך אותו פרוקסי |

הספק נבחר לפי המפתח שקיים (OpenAI מנצח אם שניהם). **המפתח לעולם לא מגיע לדפדפן.**

### חיסכון בטוקנים — איך זה עובד

1. **שכבת העברית רצה קודם, תמיד, בחינם.** מודל נקרא **רק** כשהיא לא הבינה את ההודעה.
   תשובות קצרות, לחיצות על צ'יפים, "4 ו-9", "ינואר" — לא עולים כלום.
2. **קריאה אחת לתור, לא שתיים.** ניסוח התשובה נעשה בתבניות.
3. **פרומפט רזה** (~400 טוקנים במקום ~1500) + `response_format: json_object`.
4. **היסטוריה מקוצרת** — רק 4 ההודעות האחרונות נשלחות; השאר כבר מקופל ב-slots.
5. **מונה עלות בלוג**: כל קריאה מדפיסה טוקנים ועלות מצטברת בשקלים.

בונוס בטיחות: המודל **לא רואה את המלאי בכלל**. הוא ממלא שדות בלבד, ולכן פיזית
לא יכול להמציא מלון, תאריך, מחיר או זמינות.

### הטמעה דרך Google Tag Manager

הקובץ [public/gtm-tag.html](public/gtm-tag.html) מכיל את התג המוכן. בקצרה:

1. GTM → Tags → New → **Custom HTML**
2. הדביקו את תוכן הקובץ, והחליפו את `BOT_HOST` בכתובת השרת (HTTPS!)
3. Triggering → **All Pages**
4. Save → Submit → Publish

לפני זה צריך: השרת מותקן בכתובת ציבורית עם HTTPS, ו-`ALLOWED_ORIGINS` ב-`.env`
מוגדר לדומיין של פינגווין (לא כוכבית).

### עדכון נתונים (כשמגיע קובץ התחייבויות/ליווי חדש)

שימו את הקבצים ב-`source-data/` ואז:

```bash
npm run build:data
```

## הטמעה באתר האמיתי

שורה אחת לפני סגירת ה-`</body>`:

```html
<script src="https://<host>/pingwin-bot.js" data-pingwin-bot></script>
```

הווידג'ט עצמאי לגמרי (Shadow DOM) — לא מתנגש עם ה-CSS או ה-JS של האתר.
צבעים ופונט: אובייקט `THEME` בראש `public/pingwin-bot.js` (פלייסהולדרים לצבעי פינגווין).

## ארכיטקטורה — מי מחליט מה

```
לקוח כותב חופשי ──► הבנת שפה (Claude / רג'קס) ──► slots
                                                    │
                    קוד דטרמיניסטי בלבד ◄───────────┘
                    (data/filter.js — סינון זמינות,
                     תפוסה, קייטנות, פיצול חדרים)
                                                    │
                    ניסוח בעברית ◄──────────────────┘
                    (עד 8 מועמדים; ה-AI לא בוחר ולא ממציא)
```

**היקום הסגור:** רק מה שבקובץ ההתחייבויות קיים עבור הבוט. אתר פינגווין משמש
להעשרת תוכן בלבד (יישוב, siteID, הרכבי חדרים) — לא כמקור הצעות.

## פרטיות — שני שערי PII

קובצי המקור מכילים שמות לקוחות, מספרי הזמנה ופרטי ילדים. לכן:

1. **פרסור בצד שרת בלבד** — האקסל לא מגיע לדפדפן. הניקוי קורה בתוך הפרסר.
2. **שער בילד** — `npm run build:data` נכשל אם ה-JSON הציבורי מכיל תו עברי
   בשדה חדר או רצף של 6 ספרות.
3. `source-data/` ו-`server-data/` (לידים) חסומים ב-`.gitignore`.

## טסטים (סעיף 9 בפרומפט)

```bash
node tests/run-tests.js          # פרסור + מנוע סינון (27 טסטים)
node tests/test-offline.js       # מצב דמו מקצה-לקצה (12)
node tests/test-orchestration.js # צנרת Claude עם מודל מדומה (12)
```

| דרישה | איפה מכוסה |
|---|---|
| 1. משפחה 2+2 (5 ו-9) + קייטנה → שתי קבוצות הגיל באותו שבוע, חלקי מסומן | run-tests [1] |
| 2. הודעה אחת מלאה → אפס שאלות, 3 הצעות | test-offline [2], test-orchestration [2] |
| 3. "אנחנו 2, ינואר" → שאלה אחת לכל היותר | test-offline [3] |
| 4. 6 נוסעים בלי יחידה מתאימה → שני חדרים, לא "אין תוצאות" | run-tests [4], test-offline [4] |
| 4ב. פברואר בצרפת → הסבר + חלופות | run-tests [4b], test-offline [2] |
| 4ג. ואקאנס + "שמור" → אפס הופעות | run-tests [4c] |
| 5. טסט PII על ה-JSON הציבורי | run-tests [5] + שער הבילד |
| 6. מספרי פרסור מול היעד המאושר | run-tests [6] |
| 7. prompt injection → נדחה | כללים אדומים ב-system prompt (מצב Claude); במצב דמו אין מודל שאפשר להזריק לו. בדיקה ידנית אחרי חיבור מפתח |

## נתוני חדרים מאתר פינגווין

קובץ ההתחייבויות מכיר קוד חדר ותפוסה בלבד. כל השאר — מיטות, גודל, חדרי רחצה, בסיס אירוח, הסעות וסקי פס — נלקח מדפי המלונות ב-pingwin.co.il (חוק אדום 5).

- `data/rooms-raw.json` — 40 מלונות, 103 סוגי חדרים, מילה במילה מהאתר
- `tools/merge-rooms.js` — ממזג אל `data/resorts.json` (חלק מ-`npm run build:data`), ונופל אם יש בו מספר הזמנה
- `data/room-match.js` — מתאים בין שם החדר בקובץ ("J. Suite 2+1") לחדר שבאתר. שמרני בכוונה: כששני חדרים מתאימים באותה מידה הוא מוותר ומשאיר לנציג לאמת. מתחשב בגודל החבורה ("DBL 2-4" הוא חדר אחר לזוג ולמשפחה של ארבעה)
- `config/inclusions.json` — מה כלול ומה תוספת (טיסות והסעות תמיד; סקי פס למעט בולגריה; ציוד כלול במועדוני השמש)
- `tests/test-rooms.js` — שומר שהבוט לא ממציא חדר, לא מבטיח סקי פס בבולגריה ולא נוקב מחיר ציוד

דרישות שהלקוח מציין (מיטות נפרדות, ארוחת בוקר, נסיעה קצרה מהשדה) לא רק נענות — הן משפיעות על סדר ההצעות (`_requirementScore`). הן לעולם לא מסננות הצעות החוצה — הקובץ מחליט מה פנוי, ורק הוא.

## מה עוד פתוח (TODO)

- `config/booking-url.js` — פורמט ה-URL האמיתי של מנוע ההזמנות (יש כבר siteID לכל מלון)
- `data/resorts.json` — תגיות (ספא/אפרה-סקי/מתחילים) ו"מומלץ" — לפי הנחיית תומר
- `data/pricing.json` — סיווג ₪₪/₪₪₪/₪₪₪₪ אמיתי לכל מלון
- העשרת תיאורי יישובים לכרטיסים (מאתר פינגווין בלבד)
